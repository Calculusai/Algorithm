---
title: Python3级试卷
tags:
  - 电子协会考级
  - Python三级算法
icon: tabler:rosette-number-3
createTime: 2025/03/17 13:38:41
permalink: /qceitseries/Python/python3/python31.html
---

## 试卷-2209
### 2209单选题
:::important 单选题1
十六进制数100，对应的十进制数为？（ ）(2分)

- A. 128
- B. 256 [+十六进制数100，对应的十进制数为？，代码正确的是B]
- C. 28
- D. 56
:::
[+十六进制数100，对应的十进制数为？，代码正确的是B]:
    :::note 解析
    - 十六进制数转换成十进制数的方法是：将十六进制数的每一位乘以对应的权值（16的幂次方），然后相加。
    - 十六进制数 `100` 从右往左看：
        - 第0位是 `0`，权值是 16<sup>0</sup> = 1。值为 0 * 1 = 0。
        - 第1位是 `0`，权值是 16<sup>1</sup> = 16。值为 0 * 16 = 0。
        - 第2位是 `1`，权值是 16<sup>2</sup> = 256。值为 1 * 256 = 256。
    - 将各位的值相加：0 + 0 + 256 = 256。
    - 因此，十六进制数 `100` 对应的十进制数是 `256`。
    - 也可以使用 Python 内置函数 `int()` 进行转换：`int("100", 16)` 的结果是 `256`。
    - 所以，正确选项是 B。
    :::

:::important 单选题2
下代码中，问号处应该填写的答案是哪个？（ ）
```python
>>> hex( ? )
'0xa'
>>>
```
(2分)

- A. "9"
- B. 9
- C. "10"
- D. 10 [+下图代码中，问号处应该填写的答案是哪个？，代码正确的是D]
:::
[+下图代码中，问号处应该填写的答案是哪个？，代码正确的是D]:
    :::note 解析
    - Python 内置函数 `hex()` 用于将一个 **整数** 转换为其小写十六进制字符串表示形式，并带有 "0x" 前缀。
    - 题目中 `hex()` 函数的输出结果是 `'0xa'`。
    - `'0xa'` 是十六进制表示法，其中 `a` 代表十进制中的 `10`。
    - 因此，要使 `hex()` 函数返回 `'0xa'`，输入的参数必须是整数 `10`。
    - 选项 A `"9"` 和 C `"10"` 是字符串，不是整数，`hex()` 函数不接受字符串作为参数。
    - 选项 B `9` 是整数，`hex(9)` 的结果是 `'0x9'`。
    - 选项 D `10` 是整数，`hex(10)` 的结果是 `'0xa'`。
    - 所以，正确选项是 D。
    :::

:::important 单选题3
下列4个表达式中, 答案不是整数6的是? ( ) (2分)

- A. abs(-6)
- B. int(6.88)
- C. round(5.55)
- D. min(float(6) ,9,8,7) [+下列4个表达式中,答案不是整数6的是？，代码正确的是D]
:::
[+下列4个表达式中,答案不是整数6的是？，代码正确的是D]:
    :::note 解析
    - **A. `abs(-6)`**: `abs()` 函数返回数字的绝对值。`abs(-6)` 的结果是整数 `6`。
    - **B. `int(6.88)`**: `int()` 函数将浮点数转换为整数，它会直接截断小数部分。`int(6.88)` 的结果是整数 `6`。
    - **C. `round(5.55)`**: `round()` 函数用于四舍五入。在 Python 3 中，对于 `.5` 的情况，它会圆整到最接近的偶数。`round(5.55)` 的结果是整数 `6`。
    - **D. `min(float(6), 9, 8, 7)`**:
        - `float(6)` 将整数 `6` 转换为浮点数 `6.0`。
        - `min()` 函数返回其参数中的最小值。
        - `min(6.0, 9, 8, 7)` 的参数是 `6.0`, `9`, `8`, `7`。这些参数中，`6.0` 是最小的。
        - 因此，表达式的结果是浮点数 `6.0`。
    - 题目要求找出答案 **不是整数6** 的选项。选项 A、B、C 的结果都是整数 `6`，而选项 D 的结果是浮点数 `6.0`。虽然数值上等于6，但其类型是浮点型，不是整数类型。
    - 所以，正确选项是 D。
    :::


:::important 单选题4
min()函数用于获取参数中的最小值, 如果 `a = min('654')`, 请问下面表达式中, 正确的是哪一个? ( ) (2分)

- A.
  ```python
  print(max(chr(a),3,2))
  ```
- B.
  ```python
  print(max(bin(a),3,2))
  ```
- C. [+min()函数用于获取参数中的最小值,如果请问下面表达式中,正确的是哪一个？代码正确的是C]
  ```python
  print(max(float(a),3,2))
  ```
- D.
  ```python
  print(max(hex(a),3,2))
  ```
:::
[+min()函数用于获取参数中的最小值,如果请问下面表达式中,正确的是哪一个？代码正确的是C]:
    :::note 解析
    - 首先分析 `a = min('654')`：`min()` 函数作用于字符串时，会返回字符串中 ASCII 码值最小的那个字符。字符 '6', '5', '4' 的 ASCII 码值中，'4' 的最小。因此，变量 `a` 被赋值为字符串 `'4'`。
    - **A. `print(max(chr(a),3,2))`**: `chr()` 函数需要一个整数参数（Unicode 码点），但 `a` 是字符串 `'4'`。将字符串传递给 `chr()` 会导致 `TypeError`。所以 A 错误。
    - **B. `print(max(bin(a),3,2))`**: `bin()` 函数需要一个整数参数，但 `a` 是字符串 `'4'`。将字符串传递给 `bin()` 会导致 `TypeError`。所以 B 错误。
    - **C. `print(max(float(a),3,2))`**:
        - `float(a)`: `float()` 函数可以将数字字符串转换为浮点数。`float('4')` 的结果是浮点数 `4.0`。
        - `max(float(a), 3, 2)` 变为 `max(4.0, 3, 2)`。`max()` 函数可以比较整数和浮点数，返回其中的最大值。`4.0`, `3`, `2` 中的最大值是 `4.0`。
        - `print(4.0)` 会输出 `4.0`。这个表达式可以正确执行。
    - **D. `print(max(hex(a),3,2))`**: `hex()` 函数需要一个整数参数，但 `a` 是字符串 `'4'`。将字符串传递给 `hex()` 会导致 `TypeError`。所以 D 错误。
    - 综上所述，只有选项 C 中的表达式可以正确执行。
    - 所以，正确选项是 C。
    :::

:::important 单选题5
对于CSV格式数据文件, 下列描述错误的是? ( ) (2分)

- A. CSV文件使用逗号分隔值。
- B. CSV文件的扩展名为.CS。 [+对于CSV格式数据文件,下列描述错误的是？，代码正确的是B]
- C. CSV格式是一种通用的, 相对简单的文件格式。
- D. “记事本”和“Excel”可直接读入和保存CSV格式文件。
:::
[+对于CSV格式数据文件,下列描述错误的是？，代码正确的是B]:
    :::note 解析
    - **A. CSV文件使用逗号分隔值。** CSV 的全称是 Comma-Separated Values，即逗号分隔值。这是 CSV 文件的基本定义。该描述正确。
    - **B. CSV文件的扩展名为.CS。** CSV 文件的标准扩展名是 `.csv`。`.CS` 通常是 C# 源代码文件的扩展名。该描述错误。
    - **C. CSV格式是一种通用的, 相对简单的文件格式。** CSV 格式因其结构简单、易于读写而被广泛应用于不同程序之间的数据交换。该描述正确。
    - **D. “记事本”和“Excel”可直接读入和保存CSV格式文件。** 记事本可以作为纯文本文件打开和编辑 CSV 文件。Excel 等电子表格软件能够识别 CSV 格式，并将其以表格形式展示，也支持将表格数据导出为 CSV 文件。该描述正确。
    - 题目要求选出 **错误** 的描述。根据以上分析，选项 B 是错误的。
    - 所以，正确选项是 B。
    :::

:::important 单选题6
下列程序将建立一个CSV格式文件, 划线处正确的代码选项应该为? ( )(2分)
```python
1 a=['老虎','狗','大象','牛']
2 f=open('动物.CSV',______)
3 f.write(','.join(a)+'\n')
4 f.close()
```
- A. 'W'
- B. 'R'
- C. 'w' [+下列程序将建立一个CSV格式文件,划线处正确的代码选项应该为？，代码正确的是C]
- D. 'r'
:::
[+下列程序将建立一个CSV格式文件,划线处正确的代码选项应该为？，代码正确的是C]:
    :::note 解析
    - 该程序的目标是创建一个名为 `动物.CSV` 的文件，并将列表 `a` 中的元素以逗号分隔的形式写入该文件。
    - 第 3 行代码 `f.write(...)` 表明需要对文件进行写入操作。
    - Python 的 `open()` 函数用于打开文件，其第二个参数指定了文件的打开模式。
    - `'w'` 模式表示以 **写入** (write) 模式打开文件。如果文件已存在，会先清空文件内容；如果文件不存在，则会创建新文件。这是本题所需的操作。
    - `'r'` 模式表示以 **读取** (read) 模式打开文件。如果尝试写入，会引发错误。
    - 文件模式是区分大小写的，通常使用小写字母。因此 `'W'` 和 `'R'` 不是标准的文本文件写入和读取模式。
    - 结合程序的目的（建立并写入文件），正确的模式应该是 `'w'`。
    - 所以，正确选项是 C。
    :::


:::important 单选题7
动物.CSV的文件内容如下: `'老虎','狗','大象','牛'`, 下列程序从该CSV文件读出数据, 并输出列表格式, 划线处正确的代码选项应该为? ( )
```python
1 f=open('动物.CSV','r')
2 a=f.read().strip().split(______)
3 f.close()
4 print(a)
```
(2分)

- A. `', '`
- B. `','`[+动物.CSV的文件内容如下下列程序从该CSV文件读出数据,并输出列表格式,划线处正确的代码选项应该为？，代码正确的是B]
- C. `'\n'`
- D. `'\t'`
:::
[+动物.CSV的文件内容如下下列程序从该CSV文件读出数据,并输出列表格式,划线处正确的代码选项应该为？，代码正确的是B]:
    :::note 解析
    - 程序首先以读取模式 (`'r'`) 打开 `动物.CSV` 文件。
    - `f.read()` 读取文件的全部内容，得到一个字符串，例如 `'老虎','狗','大象','牛\n'` (假设上一题写入时末尾有换行符)。
    - `.strip()` 方法移除字符串首尾的空白字符（包括换行符 `\n`），结果变为 `'老虎','狗','大象','牛'`。
    - `.split(______)` 方法用于将字符串按照指定的分隔符分割成一个列表。
    - 文件内容 `'老虎','狗','大象','牛'` 中，各个元素之间是用逗号 `,` 分隔的。
    - 因此，为了得到列表 `['老虎', '狗', '大象', '牛']`，需要使用逗号 `','` 作为 `split()` 方法的分隔符。
    - 选项 A `', '` 会按逗号+空格分割，选项 C `'\n'` 会按换行符分割，选项 D `'\t'` 会按制表符分割，均不符合文件内容的分隔方式。
    - 所以，正确选项是 B。
    :::

:::important 单选题8
下面程序生成CSV格式文件:
```text
王鑫,86,90,92,99,94,95
杨小虹,93,97,95,90,86,85
李静,89,98,99,94,90,85
```
程序如下:
```python
1 a=[['王鑫','86','90','92','99','94','95'],['杨小虹','93','97','95','90','86','85'],['李静','89','98','99','94','90','85']]
2 f=open('成绩.CSV','w')
3 for i in a:
4     f.write(','.join(i)+______)
5 f.close()
```
划线处的代码应该为? ( ) (2分)

- A. `'r'`
- B. `'w'`
- C. `'\t'`
- D. `'\n'` [+下面程序生成CSV格式文件划线处的代码应该为？，代码正确的是D]
:::
[+下面程序生成CSV格式文件划线处的代码应该为？，代码正确的是D]:
    :::note 解析
    - 程序的目标是将嵌套列表 `a` 中的数据写入 `成绩.CSV` 文件，每一行代表一个内部列表的数据，数据项之间用逗号分隔。
    - 循环 `for i in a:` 遍历列表 `a` 中的每一个内部列表 `i`。
    - `','.join(i)` 将内部列表 `i` 中的所有元素（都是字符串）用逗号连接成一个单一的字符串。例如，对于第一个内部列表，结果是 `'王鑫,86,90,92,99,94,95'`。
    - `f.write(...)` 将这个字符串写入文件。
    - 为了让每个内部列表的数据占据文件中的单独一行，需要在写入每行数据后添加一个换行符。
    - 换行符在 Python 中用 `'\n'` 表示。
    - 因此，`f.write()` 语句应该写为 `f.write(','.join(i) + '\n')`，这样每次写入一行数据后都会换到下一行。
    - 选项 A `'r'` 和 B `'w'` 是文件打开模式，不是要写入的字符。
    - 选项 C `'\t'` 是制表符，会使数据项之间以制表符分隔，而不是换行。
    - 所以，正确选项是 D。
    :::

:::important 单选题9
一个“成绩.CSV”文件（用“记事本”打开）如下：
```text
王鑫,86,90,92,99,94,95
杨小虹,93,97,95,90,86,85
李静,89,98,99,94,90,85
```
下面程序执行结果是？ ( )
```python
1 f=open('成绩.CSV','r')
2 a=[]
3 for i in f:
4     a.append(i.strip().split(','))
5 f.close()
6 print(a)
```
(2分)

- A. `[['王鑫', '86', '90', '92', '99', '94', '95'], ['杨小虹', '93', '97', '95', '90', '86', '85'], ['李静', '89', '98', '99', '94', '90', '85']]` [+一个成绩CSV文件用记事本打开如下面程序执行结果是？，代码正确的是A]
- B. `[['王鑫', '86', '90', '92', '99', '94', '95\n'], ['杨小虹', '93', '97', '95', '90', '86', '85\n'], ['李静', '89', '98', '99', '94', '90', '85\n']]`
- C. `[['王鑫,86,90,92,99,94,95'], ['杨小虹,93,97,95,90,86,85'], ['李静,89,98,99,94,90,85']]`
- D. `['王鑫,86,90,92,99,94,95\n', '杨小虹,93,97,95,90,86,85\n', '李静,89,98,99,94,90,85\n']`
:::
[+一个成绩CSV文件用记事本打开如下面程序执行结果是？，代码正确的是A]:
    :::note 解析
    - 程序以读取模式打开 `成绩.CSV` 文件。
    - `for i in f:` 循环会逐行读取文件内容。每次迭代，变量 `i` 会被赋值为文件的一行，**包含行末的换行符 `\n`** (除了可能存在的最后一行)。
    - 在循环内部：
        - `i.strip()`: 移除字符串 `i` 首尾的空白字符，特别是行末的换行符 `\n`。例如，第一行的 `'王鑫,86,90,92,99,94,95\n'` 会变成 `'王鑫,86,90,92,99,94,95'`。
        - `.split(',')`: 将经过 `strip()` 处理后的字符串按逗号 `,` 分割，生成一个包含各个数据项的字符串列表。例如，`'王鑫,86,90,92,99,94,95'` 会变成 `['王鑫', '86', '90', '92', '99', '94', '95']`。
        - `a.append(...)`: 将这个生成的列表追加到列表 `a` 中。
    - 循环结束后，列表 `a` 会包含三个列表，每个子列表对应 CSV 文件中的一行数据。
    - `print(a)` 输出最终的嵌套列表。
    - 对比选项：
        - A 正确地表示了每一行被分割成一个字符串列表，并且换行符已被移除。
        - B 错误，因为 `strip()` 移除了换行符 `\n`。
        - C 错误，因为它没有执行 `split(',')` 操作，每行仍然是一个字符串（但 `strip()` 移除了换行符）。
        - D 错误，因为它既没有执行 `strip()` 也没有执行 `split(',')`，只是将原始行（包含 `\n`）放入列表。
    - 所以，正确选项是 A。
    :::

:::important 单选题10
猜一个2022以内的随机数, 用计算机解决该问题, 比较合适的算法? ( ) (2分)
- A. 二分查找算法 [+猜一个2022以内的随机数,用计算机解决该问题,比较合适的算法?，代码正确的是A]
- B. 解析算法
- C. 枚举算法
- D. 冒泡排序算法
:::
[+猜一个2022以内的随机数,用计算机解决该问题,比较合适的算法?，代码正确的是A]:
    :::note 解析
    - **问题分析**: 这是一个典型的查找问题。我们需要在一个已知的、有序的范围（例如 1 到 2022）内找到一个特定的数字。每次猜测后，我们可以得到反馈（猜大了、猜小了还是猜对了），这可以帮助我们缩小下一次猜测的范围。
    - **A. 二分查找算法 (Binary Search)**: 这种算法通过每次猜测范围的中间值，然后根据反馈将搜索范围缩小一半。对于有序范围内的查找问题，二分查找非常高效。猜数字游戏正是二分查找的经典应用场景。
    - **B. 解析算法 (Analytic Algorithm)**: 通常指通过数学公式直接计算出结果的算法。不适用于这种需要通过尝试和反馈来寻找答案的问题。
    - **C. 枚举算法 (Enumeration Algorithm)**: 也称为穷举法。它会尝试范围内所有可能的值，直到找到目标。对于 1 到 2022 这个范围，枚举法虽然可行，但效率远低于二分查找。例如，如果目标数字是 2000，枚举法可能需要尝试 2000 次，而二分查找只需要大约 log₂(2022) ≈ 11 次。
    - **D. 冒泡排序算法 (Bubble Sort Algorithm)**: 这是一种排序算法，用于将一组数据按特定顺序排列，与查找特定数字的问题无关。
    - **结论**: 考虑到效率，二分查找算法是解决猜数字问题的最合适算法。
    - 所以，正确选项是 A。
    :::

:::important 单选题11
现在一组初始记录无序的数据`[8,9,5,2,1]`, 使用冒泡算法, 按从小到大的顺序排列, 则第三轮排序的结果为? ( ) (2分)

- A. `[8,5,2,1,9]`
- B. `[2,1,5,8,9]` [+现在一组初始记录无序的数据'8,9,5,2,1',使用冒泡算法,按从小到大的顺序排列,则第三轮排序的结果为?，代码正确的是B]
- C. `[5,2,1,8,9]`
- D. `[1,2,8,9,5]`
:::
[+现在一组初始记录无序的数据'8,9,5,2,1',使用冒泡算法,按从小到大的顺序排列,则第三轮排序的结果为?，代码正确的是B]:
    :::note 解析
    冒泡排序（从小到大）的基本思想是：重复地遍历要排序的数列，一次比较两个元素，如果它们的顺序错误就把它们交换过来。遍历数列的工作是重复地进行直到没有再需要交换，也就是说该数列已经排序完成。每一轮遍历会将当前未排序部分的最大元素放到该部分的末尾。

    初始数据: `[8, 9, 5, 2, 1]`

    **第一轮排序:**
    - 比较 `8` 和 `9`，顺序正确，不交换: `[8, 9, 5, 2, 1]`
    - 比较 `9` 和 `5`，顺序错误，交换: `[8, 5, 9, 2, 1]`
    - 比较 `9` 和 `2`，顺序错误，交换: `[8, 5, 2, 9, 1]`
    - 比较 `9` 和 `1`，顺序错误，交换: `[8, 5, 2, 1, 9]`
    第一轮结束后，最大的元素 `9` 被放到了最后。结果: `[8, 5, 2, 1, 9]`

    **第二轮排序:** (比较范围到倒数第二个元素)
    - 比较 `8` 和 `5`，顺序错误，交换: `[5, 8, 2, 1, 9]`
    - 比较 `8` 和 `2`，顺序错误，交换: `[5, 2, 8, 1, 9]`
    - 比较 `8` 和 `1`，顺序错误，交换: `[5, 2, 1, 8, 9]`
    第二轮结束后，第二大的元素 `8` 被放到了倒数第二的位置。结果: `[5, 2, 1, 8, 9]`

    **第三轮排序:** (比较范围到倒数第三个元素)
    - 比较 `5` 和 `2`，顺序错误，交换: `[2, 5, 1, 8, 9]`
    - 比较 `5` 和 `1`，顺序错误，交换: `[2, 1, 5, 8, 9]`
    第三轮结束后，第三大的元素 `5` 被放到了倒数第三的位置。结果: `[2, 1, 5, 8, 9]`

    因此，第三轮排序的结果是 `[2, 1, 5, 8, 9]`。
    所以，正确选项是 B。
    :::


:::important 单选题12
有如下列表`a=[3,5,35,74,1,28,7]`, 采用选择排序算法进行升序排序, 请问第三轮排序之后的结果是? ( ) (2分)

- A. `[1,3,5,35,74,28,7]`
- B. `[1,3,5,7,28,35,74]`
- C. `[1,3,5,74,35,28,7]` [+有如下列表a,采用选择排序算法进行升序排序,请问第三轮排序之后的结果是?，代码正确的是C]
- D. `[1,3,5,7,35,74,28]`
:::
[+有如下列表a,采用选择排序算法进行升序排序,请问第三轮排序之后的结果是?，代码正确的是C]:
    :::note 解析
    选择排序（升序）的基本思想是：每一轮从未排序的部分中找到最小（或最大）的元素，然后将其放到已排序部分的末尾。

    初始数据: `[3, 5, 35, 74, 1, 28, 7]`

    **第一轮排序:**
    - 从索引 0 开始，查找整个列表 `[3, 5, 35, 74, 1, 28, 7]` 中的最小值。
    - 最小值是 `1`，位于索引 4。
    - 将最小值 `1` 与当前轮次的起始位置（索引 0）的元素 `3` 交换。
    - 结果: `[1, 5, 35, 74, 3, 28, 7]`

    **第二轮排序:**
    - 从索引 1 开始，查找未排序部分 `[5, 35, 74, 3, 28, 7]` 中的最小值。
    - 最小值是 `3`，位于原列表索引 4 (当前子列表索引 3)。
    - 将最小值 `3` 与当前轮次的起始位置（索引 1）的元素 `5` 交换。
    - 结果: `[1, 3, 35, 74, 5, 28, 7]`

    **第三轮排序:**
    - 从索引 2 开始，查找未排序部分 `[35, 74, 5, 28, 7]` 中的最小值。
    - 最小值是 `5`，位于原列表索引 4 (当前子列表索引 2)。
    - 将最小值 `5` 与当前轮次的起始位置（索引 2）的元素 `35` 交换。
    - 结果: `[1, 3, 5, 74, 35, 28, 7]`

    因此，第三轮排序之后的结果是 `[1, 3, 5, 74, 35, 28, 7]`。
    所以，正确选项是 C。
    :::


:::important 单选题13
程序运行过程中出现的错误或意外, 不包括以下选项? ( ) (2分)

- A. 语法错误
- B. 电脑不好 [+程序运行过程中出现的错误或意外,不包括以下选项?，代码正确的是B]
- C. 运行错误
- D. 逻辑错误
:::
[+程序运行过程中出现的错误或意外,不包括以下选项?，代码正确的是B]:
    :::note 解析
    程序运行过程中可能出现的错误或意外通常分为以下几类：
    - **A. 语法错误 (Syntax Error)**: 指代码不符合编程语言的语法规则。这类错误通常在程序运行 **之前**（编译或解释阶段）就会被检测出来，导致程序无法开始执行。严格来说，它不是“运行过程中”出现的错误，但广义上属于程序开发中的错误。然而，题目问的是“运行过程中出现的错误或意外”，有些解释器可能在运行时才发现某些语法问题（虽然较少见）。但与其他选项相比，它与运行过程的关系最不直接。
    - **C. 运行错误 (Runtime Error / Exception)**: 指程序在运行期间发生的错误，导致程序异常终止。例如，除以零、访问不存在的文件、使用了未定义的变量、数组越界等。这些是典型的“运行过程中出现的错误或意外”。
    - **D. 逻辑错误 (Logic Error)**: 指程序能够正常运行并结束，但产生的结果不符合预期。这是因为程序的算法或逻辑设计有缺陷。例如，本应计算总和却计算了平均值。这也是在“运行过程中”体现出来的错误（虽然程序本身不一定会崩溃）。
    - **B. 电脑不好 (Hardware Issue)**: 指计算机硬件发生故障，例如内存损坏、硬盘故障、CPU 过热等。虽然硬件问题会导致程序运行失败或出现异常，但这 **不是程序本身** 的错误，而是外部环境的问题。题目讨论的是程序自身的错误或意外。

    题目问“不包括以下选项”，选项 B “电脑不好”描述的是硬件问题，不属于程序代码或逻辑层面的错误。语法错误、运行错误和逻辑错误都是与程序代码直接相关的错误类型。因此，“电脑不好”不属于程序运行过程中出现的（程序性）错误或意外。
    所以，正确选项是 B。
    :::


:::important 单选题14
关于Python在处理程序异常时, 下列说法不正确的是? ( ) (2分)

- A. 每一个try模块只能设定一个except模块 [+关于Python在处理程序异常时,下列说法不正确的是?，代码正确的是A]
- B. 执行except模块部分, 可以让程序继续运行
- C. 程序有错误时执行except中的代码, 没有错误时执行try中的代码
- D. 异常处理可以弥补程序漏洞, 使得程序在一些情况下不会终止运行。
:::
[+关于Python在处理程序异常时,下列说法不正确的是?，代码正确的是A]:
    :::note 解析
    - **A. 每一个try模块只能设定一个except模块**: 这个说法是 **错误** 的。一个 `try` 语句块后面可以跟一个或多个 `except` 语句块，用于捕获和处理不同类型的异常。例如：
      ```python
      try:
          # 可能引发异常的代码
          x = 1 / 0
      except ZeroDivisionError:
          print("捕获到除零错误")
      except TypeError:
          print("捕获到类型错误")
      except Exception as e:
          print(f"捕获到其他错误: {e}")
      ```
    - **B. 执行except模块部分, 可以让程序继续运行**: 这个说法是 **正确** 的。当 `try` 块中的代码引发异常，并且该异常被匹配的 `except` 块捕获时，`except` 块中的代码会执行。执行完 `except` 块后，程序通常会继续执行 `try...except` 结构之后的代码，而不是直接终止。
    - **C. 程序有错误时执行except中的代码, 没有错误时执行try中的代码**: 这个说法 **基本正确地描述了流程**。程序总是先尝试执行 `try` 块中的代码。如果 `try` 块中 **发生** 了错误（异常），则 `try` 块中剩余的代码会被跳过，然后执行匹配的 `except` 块中的代码。如果 `try` 块中 **没有** 发生错误，则 `try` 块的代码会完整执行，并且所有的 `except` 块都会被跳过。虽然表述“没有错误时执行try”不完全精确（因为try总是先执行），但它传达了核心机制。
    - **D. 异常处理可以弥补程序漏洞, 使得程序在一些情况下不会终止运行**: 这个说法是 **正确** 的。异常处理的主要目的之一就是提高程序的健壮性（Robustness）。通过捕获预期的或意外的错误，程序可以执行备用逻辑、记录错误信息或进行清理操作，而不是直接崩溃，从而在某些错误情况下避免程序终止。

    题目要求选出 ==不正确== 的说法，因此选项 A 是正确答案。
    所以，正确选项是 A。
    :::

:::important 单选题15
`divmod()`函数的功能是用来求商和计算余数。对应变量x和y, `divmod(x,y)`返回的结果是以下哪一项? ( ) (2分)

- A. `(x//y, x%y)` [+divmod()函数的功能是用来求商和计算余数。对应变量x和y,divmod(x,y)返回的结果是以下哪一项?，代码正确的是A]
- B. `(x/y, x%y)`
- C. `(x%y, x//y)`
- D. `(x%y, x/y)`
:::
[+divmod()函数的功能是用来求商和计算余数。对应变量x和y,divmod(x,y)返回的结果是以下哪一项?，代码正确的是A]:
    :::note 解析
    - Python 内置函数 `divmod(a, b)` 接收两个数字（非复数）作为参数，并返回一个包含商和余数的元组 `(quotient, remainder)`。
    - 这个函数执行的是 **整数除法**（也称为地板除法）。
    - 商 `quotient` 等同于 `a // b` 的结果。
    - 余数 `remainder` 等同于 `a % b` 的结果。
    - 因此，`divmod(x, y)` 返回的结果是 `(x // y, x % y)`。
    - 选项 A 正确表示了整数除法的商和余数组成的元组。
    - 选项 B 使用了浮点除法 `/`，不符合 `divmod` 的行为。
    - 选项 C 和 D 的顺序错误，并且 D 还使用了浮点除法。
    - 所以，正确选项是 A。
    :::


:::important 单选题16
`bool()`函数用于将给定参数或表达式转换为布尔类型, 以下使用了`bool()`函数的实例中, 哪项返回True值? ( ) (2分)

- A. `bool(0)`
- B. `bool( )`
- C. `bool(15*15+14*14<420)`
- D. `bool(-1)` [+bool()函数用于将给定参数或表达式转换为布尔类型,以下使用了bool()函数的实例中,哪项返回True值?，代码正确的是D]
:::
[+bool()函数用于将给定参数或表达式转换为布尔类型,以下使用了bool()函数的实例中,哪项返回True值?，代码正确的是D]:
    :::note 解析
    - Python 的 `bool()` 函数用于将一个值转换为布尔值 (`True` 或 `False`)。
    - 在 Python 中，以下值被认为是“假” (Falsy)，转换后为 `False`：
        - `None`
        - `False`
        - 任何数值类型的零，如 `0`, `0.0`, `0j`
        - 任何空序列，如 `''` (空字符串), `()` (空元组), `[]` (空列表)
        - 任何空映射，如 `{}` (空字典)
        - 用户定义的类实例，如果该类定义了 `__bool__()` 或 `__len__()` 方法且返回 `False` 或 `0`。
    - 所有其他值都被认为是“真” (Truthy)，转换后为 `True`。

    - **A. `bool(0)`**: 整数 `0` 是数值类型的零，所以 `bool(0)` 返回 `False`。
    - **B. `bool()`**: 不带参数调用 `bool()` 返回 `False`。
    - **C. `bool(15*15+14*14<420)`**:
        - 先计算表达式 `15*15+14*14<420`。
        - `15*15 = 225`
        - `14*14 = 196`
        - `225 + 196 = 421`
        - 表达式变为 `421 < 420`。
        - 这个比较的结果是 `False`。
        - 因此，`bool(False)` 返回 `False`。
    - **D. `bool(-1)`**: 整数 `-1` 是非零数值，所以 `bool(-1)` 返回 `True`。

    - 题目要求找出返回 `True` 值的选项。根据以上分析，只有选项 D 返回 `True`。
    - 所以，正确选项是 D。
    :::

:::important 单选题17
查看对象的属性和属性值等信息, 可以使用以下哪种函数? ( ) (2分)

- A. `vars()` [+查看对象的属性和属性值等信息,可以使用以下哪种函数?，代码正确的是A]
- B. `dir()`
- C. `help()`
- D. `map()`
:::
[+查看对象的属性和属性值等信息,可以使用以下哪种函数?，代码正确的是A]:
    :::note 解析
    - **A. `vars([object])`**:
        - 如果不带参数，`vars()` 返回当前本地作用域的字典（类似 `locals()`）。
        - 如果带有参数（一个模块、类、实例或任何具有 `__dict__` 属性的对象），`vars()` 返回该对象的 `__dict__` 属性。`__dict__` 是一个字典，存储了对象的可写属性及其对应的值。这完全符合题目描述的“查看对象的属性和属性值”。
    - **B. `dir([object])`**:
        - 如果不带参数，`dir()` 返回当前本地作用域中的名称列表。
        - 如果带有参数，`dir()` 尝试返回该对象的有效属性列表（主要是属性名和方法名）。它主要用于查看对象 **有哪些** 属性，但不直接显示属性的 **值**。
    - **C. `help([object])`**:
        - `help()` 函数调用内置的帮助系统。它会显示对象的文档字符串、方法签名等帮助信息，虽然可能包含属性信息，但其主要目的是提供文档说明，而不是以简洁的键值对形式列出属性及其当前值。
    - **D. `map(function, iterable, ...)`**:
        - `map()` 函数将一个函数应用于可迭代对象的每个元素，并返回一个迭代器。它用于数据转换，与查看对象属性无关。

    - 题目要求查找能够查看对象“属性和属性值”的函数。`vars()` 函数直接返回包含属性名和对应值的字典 (`__dict__`)，是最符合要求的。
    - 所以，正确选项是 A。
    :::


:::important 单选题18
`round(20/3)` 的返回值是以下哪一项? ( ) (2分)

- A. 6
- B. 2
- C. 7 [+round(20/3)的返回值是以下哪一项?，代码正确的是C]
- D. 1
:::
[+round(20/3)的返回值是以下哪一项?，代码正确的是C]:
    :::note 解析
    - 首先计算括号内的表达式 `20/3`。在 Python 3 中，`/` 执行的是浮点数除法。
    - `20 / 3` 的结果大约是 `6.666666...`。
    - `round(number)` 函数将数字 `number` 四舍五入到最接近的整数。
    - 对于 `6.666666...`，最接近的整数是 `7`。
    - 因此，`round(20/3)` 即 `round(6.666666...)` 的结果是 `7`。
    - 所以，正确选项是 C。
    :::

:::important 单选题19
语句`sorted([9,6,8,2,5], reverse = True)`的返回结果是以下哪一项？ ( ) (2分)

- A. `[2,5,6,8,9]`
- B. `[9,6,8,2,5]`
- C. `[9,8,6,5,2]` [+语句sorted的返回结果是以下哪一项？，代码正确的是C]
- D. `[0]`
:::
[+语句的返回结果是以下哪一项？，代码正确的是C]:
    :::note 解析
    - Python 内置函数 `sorted(iterable, *, key=None, reverse=False)` 用于对可迭代对象 `iterable` 中的元素进行排序，并返回一个新的已排序列表。原始的可迭代对象不会被修改。
    - 参数 `iterable` 是要排序的对象，这里是列表 `[9, 6, 8, 2, 5]`。
    - 参数 `reverse` 是一个布尔值。如果设置为 `True`，则列表元素将按降序（从大到小）排序。如果为 `False`（默认值），则按升序（从小到大）排序。
    - 在本题中，`reverse=True`，表示需要对列表 `[9, 6, 8, 2, 5]` 进行降序排序。
    - 将列表 `[9, 6, 8, 2, 5]` 中的元素从大到小排列，得到 `[9, 8, 6, 5, 2]`。
    - 因此，`sorted([9, 6, 8, 2, 5], reverse=True)` 的返回结果是 `[9, 8, 6, 5, 2]`。
    - 所以，正确选项是 C。
    :::


:::important 单选题20
执行语句`"{1}{0}".format("中国", "加油", "!")`, 输出结果是以下哪一项？ ( ) (2分)

- A. `'中国加油!'`
- B. `'加油中国!'` 
- C. `'中国加油'`
- D. `'加油中国'`[+执行语句format,输出结果是以下哪一项？，代码正确的是D]
:::
[+执行语句format,输出结果是以下哪一项？，代码正确的是D]:
    :::note 解析
    - Python 的字符串 `format()` 方法用于格式化字符串。
    - 格式字符串 `"{1}{0}"` 中的 `{}` 是占位符。
    - 占位符中的数字表示 `format()` 方法参数的位置索引。参数 `"中国"` 的索引是 0，参数 `"加油"` 的索引是 1。
    - `{1}` 会被 `format()` 的第 1 个参数（索引从 0 开始）替换，即 `"加油"`。
    - `{0}` 会被 `format()` 的第 0 个参数替换，即 `"中国"`。
    - 因此，字符串 `"{1}{0}"` 经过格式化后，会变成 `"加油"` + `"中国"`。
    - 最终的输出结果是字符串 `'加油中国'`。
    - 所以，正确选项是 D。
    :::

:::important 单选题21
关于语句`float(2022)`与`float('2022')`运行后的输出结果, 以下哪一项正确? ( ) (2分)

- A. 均为2022
- B. 均为2022.0 [+关于语句float与float运行后的输出结果,以下哪一项正确?，代码正确的是B]
- C. `float(2022)`的输出为2022, `float('2022')`运行后出错
- D. `float(2022)`的输出为2022.0, `float('2022')`运行后出错
:::
[+关于语句float与float运行后的输出结果,以下哪一项正确?，代码正确的是B]:
    :::note 解析
    - Python 内置函数 `float(x)` 用于将数字或字符串 `x` 转换为浮点数。
    - **`float(2022)`**: 参数 `2022` 是一个整数。`float()` 函数会将整数转换为等值的浮点数。因此，`float(2022)` 的结果是 `2022.0`。
    - **`float('2022')`**: 参数 `'2022'` 是一个字符串。`float()` 函数可以解析包含数字表示的字符串，并将其转换为浮点数。字符串 `'2022'` 是一个有效的数字表示。因此，`float('2022')` 的结果也是 `2022.0`。
    - 两个语句的运行结果都是浮点数 `2022.0`。
    - 所以，正确选项是 B。
    :::

:::important 单选题22
语句`max([(1,2),(2,3),(3,4),(2,5)])`运行后的输出结果是以下哪一项？ ( ) (2分)

- A. 5
- B. (2,5)
- C. (4,5)
- D. (3, 4) [+语句max运行后的输出结果是以下哪一项？，代码正确的是D]
:::
[+语句max运行后的输出结果是以下哪一项？，代码正确的是D]:
    :::note 解析
    - Python 内置函数 `max(iterable)` 用于返回可迭代对象 `iterable` 中的最大元素。
    - 当 `max()` 函数应用于包含元组的列表时，它会进行 **字典序比较 (lexicographical comparison)** 来确定哪个元组是“最大”的。
    - 字典序比较的工作方式是：
        1. 首先比较每个元组的第一个元素。第一个元素最大的元组被认为是最大的。
        2. 如果第一个元素相同，则比较第二个元素，以此类推。
    - 在列表 `[(1,2), (2,3), (3,4), (2,5)]` 中：
        - 比较第一个元素：1, 2, 3, 2。其中最大的是 3。
        - 包含最大第一个元素 3 的元组是 `(3, 4)`。
        - 由于 `(3, 4)` 的第一个元素已经是所有元组中最大的，它就是整个列表中的最大元组。
    - 因此，`max([(1,2),(2,3),(3,4),(2,5)])` 的结果是 `(3, 4)`。
    - 所以，正确选项是 D。
    :::

:::important 单选题23
`enumerate()`函数用于将一个可遍历的数据对象(如列表、元组或字符串)组合为一个索引序列。下列程序
```python
1 │ example = '7890'
2 │ for i,j in enumerate(example):
3 │     print(i,j)
```
运行后的输出打印结果为以下哪一项？ ( ) (2分)

- A.
  ```
  0,0
  1,7
  2,8
  3,9
  ```
- B.
  ```
  0,7
  1,8
  2,9
  3,0
  ```
- C.
  ```
  0 0
  1 7
  2 8
  3 9
  ```
- D. [+enumerate函数用于将一个可遍历的数据对象组合为一个索引序列下列程序运行后的输出打印结果为以下哪一项？，代码正确的是D]
  ```
  0 7
  1 8
  2 9
  3 0
  ```
:::
[+enumerate函数用于将一个可遍历的数据对象组合为一个索引序列下列程序运行后的输出打印结果为以下哪一项？，代码正确的是D]:
    :::note 解析
    - Python 内置函数 `enumerate(iterable, start=0)` 用于将一个可迭代对象（如字符串、列表、元组等）转换为一个枚举对象。
    - 在 `for` 循环中迭代 `enumerate(example)` 时，它会为 `example` 中的每个元素生成一个包含 **索引** 和 **元素值** 的元组。默认情况下，索引从 0 开始。
    - 字符串 `example = '7890'` 是一个可迭代对象。
    - 循环 `for i, j in enumerate(example):` 会依次执行以下操作：
        1. 第一次迭代：`enumerate` 产生 `(0, '7')`。`i` 被赋值为 `0`，`j` 被赋值为 `'7'`。`print(i, j)` 输出 `0 7`。
        2. 第二次迭代：`enumerate` 产生 `(1, '8')`。`i` 被赋值为 `1`，`j` 被赋值为 `'8'`。`print(i, j)` 输出 `1 8`。
        3. 第三次迭代：`enumerate` 产生 `(2, '9')`。`i` 被赋值为 `2`，`j` 被赋值为 `'9'`。`print(i, j)` 输出 `2 9`。
        4. 第四次迭代：`enumerate` 产生 `(3, '0')`。`i` 被赋值为 `3`，`j` 被赋值为 `'0'`。`print(i, j)` 输出 `3 0`。
    - `print(i, j)` 默认会在输出 `i` 和 `j` 之间添加一个空格，并在每次打印后换行。
    - 对比选项，选项 D 的输出与上述分析完全一致。选项 A、B、C 的格式或值不正确。
    - 所以，正确选项是 D。
    :::

:::important 单选题24
关于`all()`函数的用法, 以下语句中能够返回True值的是哪一项？ ( ) (2分)

- A. `all(0)`
- B. `all([0])`
- C. `all([0, 1, 2])`
- D. `all(['0', '1', '2'])` [+关于all函数的用法,以下语句中能够返回True值的是哪一项？，代码正确的是D]
:::
[+关于all函数的用法,以下语句中能够返回True值的是哪一项？，代码正确的是D]:
    :::note 解析
    - Python 内置函数 `all(iterable)` 用于判断给定的可迭代对象 `iterable` 中的所有元素是否都为 `True`（或者等价于 `True` 的值）。
    - 如果 `iterable` 的所有元素都为真值 (Truthy)，或者 `iterable` 为空，则 `all()` 返回 `True`。
    - 如果 `iterable` 中至少有一个元素为假值 (Falsy)，则 `all()` 返回 `False`。
    - Python 中的假值包括：`False`、`None`、任何数值类型的零 (`0`, `0.0`, `0j`)、任何空序列 (`''`, `()`, `[]`)、任何空映射 (`{}`) 等。

    - **A. `all(0)`**: 参数 `0` 是一个整数，不是可迭代对象。调用 `all(0)` 会引发 `TypeError: 'int' object is not iterable`。
    - **B. `all([0])`**: 可迭代对象是 `[0]`。其中包含元素 `0`。整数 `0` 是假值。因此，`all([0])` 返回 `False`。
    - **C. `all([0, 1, 2])`**: 可迭代对象是 `[0, 1, 2]`。其中包含元素 `0`，它是一个假值。因此，`all([0, 1, 2])` 返回 `False`。
    - **D. `all(['0', '1', '2'])`**: 可迭代对象是 `['0', '1', '2']`。其中包含的元素是字符串 `'0'`, `'1'`, `'2'`。在 Python 中，非空字符串都是真值。由于所有元素都是真值，`all(['0', '1', '2'])` 返回 `True`。

    - 题目要求找出返回 `True` 值的选项。根据以上分析，只有选项 D 返回 `True`。
    - 所以，正确选项是 D。
    :::


:::important 单选题25
如果要设计班级通讯录管理功能，需要往address.csv文件中追加新记录，则应该使用以下哪一种打开文件的方式？ ( ) (2分)

- A. `open('address.csv', 'a+')` [+如果要设计班级通讯录管理功能，需要往address.csv文件中追加新记录，则应该使用以下哪一种打开文件的方式？，代码正确的是A]
- B. `open('address.csv', 'w+')`
- C. `open('address.csv', 'wb+')`
- D. `open('address.csv', 'rb+')`
:::
[+如果要设计班级通讯录管理功能，需要往address.csv文件中追加新记录，则应该使用以下哪一种打开文件的方式？，代码正确的是A]:
    :::note 解析
    - 题目要求是向 CSV 文件 ==追加== 新记录。CSV 文件通常是文本文件。
    - Python 的 `open()` 函数的第二个参数指定文件打开模式：
        - `'r'`：读取（默认）。
        - `'w'`：写入（会覆盖已有内容或创建新文件）。
        - `'a'`：追加（在文件末尾写入，如果文件不存在则创建）。
        - `'x'`：独占创建（仅创建新文件，如果文件已存在则失败）。
        - `'+'`：更新（读写）。
        - `'b'`：二进制模式。
        - `'t'`：文本模式（默认）。
    - **A. `'a+'`**: 以 **追加** 和 **读取** 模式打开。写入时从文件末尾开始，允许读取。如果文件不存在则创建。这符合追加记录的需求，并且是文本模式。
    - **B. `'w+'`**: 以 **写入** 和 **读取** 模式打开。会 **覆盖** 原有文件内容，不符合追加的要求。
    - **C. `'wb+'`**: 以 **二进制写入** 和 **读取** 模式打开。会覆盖原有内容，且是二进制模式，不适合操作 CSV 文本文件。
    - **D. `'rb+'`**: 以 **二进制读取** 和 **写入** 模式打开。文件指针在开头，且是二进制模式，不适合追加文本记录。
    - 因此，最适合追加新记录到 CSV 文件的模式是 `'a'` 或 `'a+'`。选项中提供了 `'a+'`。
    - 所以，正确选项是 A。
    :::

### 2209判断题
:::important 判断题1
有这样一个表达式:
```python
1 │ ord('a') - ord('A')
```
这个表达式运行的结果是整数 32。 ( ) (2分)

- 正确 [+有这样一个表达式这个表达式运行的结果是整数32，代码正确的是正确]
- 错误
:::
[+有这样一个表达式这个表达式运行的结果是整数32，代码正确的是正确]:
    :::note 解析
    - Python 内置函数 `ord(c)` 返回表示单个字符 `c` 的 Unicode 码点的整数。
    - 在标准的 ASCII 编码（也是 Unicode 的子集）中：
        - 字符 `'a'` 的码点是 97。所以 `ord('a')` 返回整数 `97`。
        - 字符 `'A'` 的码点是 65。所以 `ord('A')` 返回整数 `65`。
    - 表达式 `ord('a') - ord('A')` 计算的是 `97 - 65`。
    - `97 - 65 = 32`。
    - 因此，该表达式运行的结果确实是整数 `32`。
    - 所以，该描述是 **正确** 的。
    :::
:::important 判断题2
二维数据的存储常常采用空格、逗号、退号、换行、分号等符号分隔元素。（ ）（2分）

- 正确 [+二维数据的存储常常采用空格、逗号、退号、换行、分号等符号分隔元素，代码正确的是正确]
- 错误
:::
[+二维数据的存储常常采用空格、逗号、退号、换行、分号等符号分隔元素，代码正确的是正确]:
    :::note 解析
    - 在实际的数据存储和文件格式（如CSV、TSV、TXT等）中，常用空格、逗号、制表符（退号）、换行、分号等作为分隔符来区分不同的数据元素，这样便于数据的读取和解析。
    - 例如CSV文件用逗号分隔，TSV文件用制表符分隔，文本表格常用空格或分号分隔。
    - 因此题干描述是符合实际应用的，答案为“正确”。
    :::

:::important 判断题3
```python
a=[
    ['王鑫','86','90','92','99','94','95'],
    ['杨小虹','93','97','95','90','86','85'],
    ['李静','89','98','99','94','90','85']
    ]
``` 
其中，杨小虹同学的第三门成绩95的数据类型是整数。（ ）（2分）

- 正确
- 错误 [+a中杨小虹同学的第三门成绩95的数据类型是整数，代码正确的是错误]
:::
[+a中杨小虹同学的第三门成绩95的数据类型是整数，代码正确的是错误]:
    :::note 解析
    - 在列表 a 中，所有成绩都被单引号包裹，属于字符串类型，而不是整数类型。
    - 例如，`a[1][2]` 实际为字符串 '95'，不是整数 95。
    - 如果需要将其转换为整数，需要使用 `int(a[1][2])`。
    - 因此，题干说法错误，答案为“错误”。
    :::
:::important 判断题4
二维数据的处理等同于二维列表的操作，借助循环遍历可实现对每个数据的处理。（ ）（2分）

- 正确 [+二维数据的处理等同于二维列表的操作，借助循环遍历可实现对每个数据的处理，代码正确的是正确]
- 错误
:::
[+二维数据的处理等同于二维列表的操作，借助循环遍历可实现对每个数据的处理，代码正确的是正确]:
    :::note 解析
    - 在Python等编程语言中，二维数据通常用二维列表（嵌套列表）来表示。
    - 对二维数据的处理本质上就是对二维列表的操作，可以通过嵌套循环遍历每个元素，实现逐个处理。
    - 例如：for row in data: for item in row: process(item)
    - 所以，题干说法正确，答案为“正确”。
    :::
:::important 判断题5
关于程序的异常处理，可以使用try...except...或try...except...except...语句进行捕获控制。（ ）（2分）

- 正确 [+关于程序的异常处理，可以使用try...except...或try...except...except...语句进行捕获控制，代码正确的是正确]
- 错误
:::
[+关于程序的异常处理，可以使用try...except...或try...except...except...语句进行捕获控制，代码正确的是正确]:
    :::note 解析
    - 在Python中，异常处理通常使用try...except...结构。
    - 可以有一个except，也可以有多个except分支，分别捕获不同类型的异常。
    - 例如：
        ```python
        try:
            # code
        except ValueError:
            # handle ValueError
        except Exception:
            # handle other exceptions
        ```
    - 所以，题干说法正确，答案为“正确”。
    :::
:::important 判断题6
使用input()函数可以一次从键盘输入一个字符串，按回车键结束输入。（ ）（2分）

- 正确 [+使用input()函数可以一次从键盘输入一个字符串，按回车键结束输入，代码正确的是正确]
- 错误
:::
[+使用input()函数可以一次从键盘输入一个字符串，按回车键结束输入，代码正确的是正确]:
    :::note 解析
    - Python 的 input() 函数用于从标准输入（通常是键盘）读取一行文本，输入以回车键结束。
    - 每次调用 input()，只能输入一行字符串，回车后输入结束，返回值类型为字符串。
    - 所以，题干说法正确，答案为“正确”。
    :::
:::important 判断题7
set是一个不允许元素重复的集合。由于set里的元素位置允许随意，所以不能用索引引访问。（ ）（2分）

- 正确 [+set是一个不允许元素重复的集合。由于set里的元素位置允许随意，所以不能用索引引访问，代码正确的是正确]
- 错误
:::
[+set是一个不允许元素重复的集合。由于set里的元素位置允许随意，所以不能用索引引访问，代码正确的是正确]:
    :::note 解析
    - Python中的set集合不允许元素重复，且元素在集合中的存储顺序是无序的。
    - 由于无序，set不支持通过索引（如set[0]）访问元素。
    - 只能通过遍历等方式访问集合中的元素。
    - 所以，题干说法正确，答案为“正确”。
    :::
:::important 判断题8
help()函数可用来查看函数或模块的帮助信息，但不能直接查看对象里所提供方法的帮助信息。（ ）（2分）

- 正确
- 错误 [+help()函数可用来查看函数或模块的帮助信息，但不能直接查看对象里所提供方法的帮助信息，代码正确的是错误]
:::
[+help()函数可用来查看函数或模块的帮助信息，但不能直接查看对象里所提供方法的帮助信息，代码正确的是错误]:
    :::note 解析
    - Python 的 help() 函数不仅可以查看函数或模块的帮助信息，也可以直接查看对象及其方法的帮助信息。
    - 例如，help(str.upper) 可以查看字符串对象的 upper 方法的帮助信息。
    - 所以，题干说法错误，答案为“错误”。
    :::
:::important 判断题9
sum()函数可以对列表进行求和，但不能对元组进行求和。（ ）（2分）

- 正确
- 错误 [+sum()函数可以对列表进行求和，但不能对元组进行求和，代码正确的是错误]
:::
[+sum()函数可以对列表进行求和，但不能对元组进行求和，代码正确的是错误]:
    :::note 解析
    - Python 的 sum() 函数不仅可以对列表进行求和，也可以对元组进行求和。
    - 例如，sum((1, 2, 3)) 的结果是 6。
    - 所以，题干说法错误，答案为“错误”。
    :::
:::important 判断题10
在Python编程语言中，'0b10' 表示二进制数10，并且这个数换算为十进制，就是整数2。（2分）

- 正确 [+在Python编程语言中，'0b10'表示二进制数10，并且这个数换算为十进制，就是整数2，代码正确的是正确]
- 错误
:::
[+在Python编程语言中，'0b10'表示二进制数10，并且这个数换算为十进制，就是整数2，代码正确的是正确]:
    :::note 解析
    - 在Python中，前缀 '0b' 表示二进制数。
    - '0b10' 表示二进制的10，换算为十进制是2。
    - 所以，题干说法正确，答案为“正确”。
    :::
### 2209编程题
::::important 1. 计算所有库存书籍的总价 (4分)
- 如下图有一个名为“book.csv”的文件，小明想计算所有库存书籍的总价，于是编写了下面代码。请将红色①②处的代码补充完整。

:::code-tabs
@tab Python
```python :collapsed-lines=5
b = 0
s = 0
with open('/data/book.csv', 'r', encoding='utf-8') as f:
    for line in f:  # ① 逐行读取文件内容
        a = line.strip("\n").split(",")
        b = b+1
        if b > 1:  # 第一行是标题，所以从第二行开始计算
            # ② 将每本书的价格与库存量相乘并累加到总价中
            s = s + int(a[1]) * int(a[2])
print('总价：', s)
```
:::
::::
::::important 2. 二分法对关键字14查找 (10分)
- 小敏想用二分法对关键字14进行查找，请你帮她补全代码。

:::code-tabs
@tab Python 
```python :collapsed-lines=5
lst = [3,12,7,45,9,10,31,90,1,23,14,52]  # 原始无序列表
a = sorted(lst)  # ① 先排序，二分查找必须在有序序列上进行
key = 14
n = len(a)
i, j = 0, n-1
flag = -1
while i <= j:  # ② 二分查找的循环条件，左右指针未交错时继续查找
    mid = (i + j) // 2
    if key == a[mid]:
        flag = mid
        break
    elif key > a[mid]:
        i = mid + 1  # ③ 关键字比中值大，查右半部分
    else:
        j = mid - 1  # ④ 关键字比中值小，查左半部分
if flag == -1:
    print(str(key) + '没找到！')
else:
    print(str(key) + '已找到！')
```
:::
::::
::::important 3. 判断考试答题卡的填涂 (16分)
- 标准考试答题卡一般采用2B铅笔填涂，填涂好的答题卡经过扫描后得到对应的数字化图像，再通过计算机识别。若分n个信息点输入RGB颜色值，判断其是否为黑色像素点（灰度值小于等于132为黑色，否则为白色），请补全下列程序。

:::code-tabs
@tab Python
```python :collapsed-lines=5
n = int(input("请输入信息点个数: "))
count = 0
for i in range(1, n+1):
    R = int(input("请输入红色分量: "))
    G = int(input("请输入绿色分量: "))
    B = int(input("请输入蓝色分量: "))
    # ① 计算灰度值，按照题目给定的公式
    Gray_scale = 0.299 * R + 0.587 * G + 0.114 * B  # 必须用题目给定的加权公式
    # ② 判断灰度值是否小于等于132，若是则为黑色，否则为白色
    if Gray_scale <= 132:  # 只有灰度值小于等于132才算黑色
        print("黑色")
        count = count + 1  # 统计黑色像素点数量
    else:
        print("白色")
print("黑色像素总共有：" + str(count) + "个")
```
:::
::::
## 试卷2212
### 2212单选题
:::important 单选题1
列表L1中全是整数，小明想将其中所有奇数都增加1，偶数不变，假设写了如下代码。请问，图中红线处，代码应该是？（2分）

```python
L1 = [3,2,5,6,4,7,3,9,11,17,19,12]
L2 = [x+1 if (x % 2) != 0 else x for x in L1]
print(L2)
```
- A. `x//2`
- B. `x^2`
- C. `x&&2`
- D. `x%2` [+列表L1中全是整数，小明想将其中所有奇数都增加1，偶数不变，代码正确的是D]
:::
[+列表L1中全是整数，小明想将其中所有奇数都增加1，偶数不变，代码正确的是D]:
    :::note 解析
    - 判断一个数是否为奇数，最常用的做法是 `x % 2 != 0`，即对2取余不等于0。
    - 所以红线处应填写 `x % 2`。
    - 选项A `x//2` 是整除，B `x^2` 是按位异或，C `x&&2` 不是Python语法，只有D正确。
    :::

:::important 单选题2
小明为了学习选择排序的算法，编写了下面的代码。针对代码中红色文字所示的一、二、三处，下面说法正确的是？（2分）

```python
a = [8,4,11,3,9]
count = len(a)
for i in range(count-1):
    mi = i
    for j in range(i+1, count):
        if a[mi] > a[j]:   # 代码一
            mi = j         # 代码二
    if i != mi:
        a[mi], a[i] = a[i], a[mi]   # 代码三
print(a)
```

- A. 如果找到更大的元素，则记录它的索引号。
- B. 如果找到更小的元素，则记录它的索引号。 [+选择排序算法代码，正确说法是B]
- C. 在一趟选择排序后，不管是否找到更小的元素，mi所在元素都与i所在的元素发生交换。
- D. 代码三所在的行必然要运行。
:::
[+选择排序算法代码，正确说法是B]:
    :::note 解析
    - 选择排序的核心思想是：每一趟从未排序部分中找到最小的元素，并记录其索引（代码一和代码二），最后与当前位置交换（代码三）。
    - 只有当找到更小的元素时才会更新mi的值（mi = j），所以B正确。
    - A描述错误，选择排序是找最小的元素，不是更大的。
    - C描述错误，只有i != mi时才交换，否则不交换。
    - D描述错误，只有i != mi时才会执行交换语句。
    :::

:::important 单选题3
小明编写了一段演示插入排序的代码，代码如下。请问红色“缺失代码”处，应该填写哪段代码？（2分）

```python
a = [8,4,11,3,9]
count = len(a)
for i in range(1, count):
    j = i
    b = a[i]
    while j > 0 and b < a[j-1]:
        a[j] = a[j-1]
        # 缺失代码
    a[j] = b
print(a)
```

- A. `j=j-1` [+插入排序缺失代码应为j=j-1，代码正确的是A]
- B. `j=j+1`
- C. `j=i+1`
- D. `j=i-1`
:::
[+插入排序缺失代码应为j=j-1，代码正确的是A]:
    :::note 解析
    - 插入排序的核心思想是在已排序序列中从后向前扫描，找到合适的位置插入当前元素。
    - while 循环每次将元素后移一位，j 需要不断向前移动（减1），直到找到插入位置。
    - 所以缺失代码应为 j = j - 1。
    - 其它选项均不符合插入排序的逻辑。
    :::
:::important 单选题4
在计算机中，信息都是采用什么进行存储？（ ）（2分）

- A. 二进制数 [+在计算机中，信息都是采用什么进行存储？代码正确的是A]
- B. 八进制数
- C. 十进制数
- D. 十六进制数
:::
[+在计算机中，信息都是采用什么进行存储？代码正确的是A]:
    :::note 解析
    - 计算机的基本存储和处理单位是二进制位（bit），所有信息最终都以0和1的形式存储和处理。
    - 八进制、十进制、十六进制只是人类为了方便阅读和表达而采用的进制方式，计算机内部并不直接以这些进制存储数据。
    - 所以，正确选项是A。
    :::
:::important 单选题5
十进制数（100）₁₀，转化为二进制数为（   ）₂？（2分）

- A. 0010011
- B. 1010001
- C. 1100100 [+十进制数100转化为二进制数，代码正确的是C]
- D. 0101100
:::
[+十进制数100转化为二进制数，代码正确的是C]:
    :::note 解析
    - 十进制数 100 转换为二进制数的方法是不断除以2取余，逆序排列余数。
    - 100 ÷ 2 = 50 ... 0
    - 50 ÷ 2 = 25 ... 0
    - 25 ÷ 2 = 12 ... 1
    - 12 ÷ 2 = 6 ... 0
    - 6 ÷ 2 = 3 ... 0
    - 3 ÷ 2 = 1 ... 1
    - 1 ÷ 2 = 0 ... 1
    - 逆序排列余数：1100100
    - 所以，正确选项是 C。
    :::
:::important 单选题6
十六进制数每一位至多可以表示几位二进制位？（ ）（2分）

- A. 2
- B. 3
- C. 4 [+十六进制数每一位至多可以表示几位二进制位？代码正确的是C]
- D. 16
:::
[+十六进制数每一位至多可以表示几位二进制位？代码正确的是C]:
    :::note 解析
    - 十六进制的每一位可以表示16种不同的数值（0~F），而二进制的4位可以表示16种不同的数值（0000~1111）。
    - 因此，1位十六进制数等价于4位二进制数。
    - 所以，正确选项是C。
    :::
:::important 单选题7
八进制数（35）₈，转化为十进制数为（   ）₁₀？（2分）

- A. 100011
- B. 110001
- C. 232
- D. 29 [+八进制数35转化为十进制数，代码正确的是D]
:::
[+八进制数35转化为十进制数，代码正确的是D]:
    :::note 解析
    - 八进制数 35 转换为十进制数的方法是：3×8¹ + 5×8⁰ = 3×8 + 5×1 = 24 + 5 = 29。
    - 所以，正确选项是 D。
    :::
:::important 单选题8
执行代码 `a = min(3, 2, 4.3)`，变量 a 的值是？（ ）（2分）

- A. 3
- B. 2 [+执行代码a=min(3,2,4.3)，变量a的值是？代码正确的是B]
- C. 4.3
- D. 4
:::
[+执行代码a=min(3,2,4.3)，变量a的值是？代码正确的是B]:
    :::note 解析
    - min() 函数返回参数中的最小值。
    - min(3, 2, 4.3) 的最小值是 2。
    - 所以，正确选项是 B。
    :::
:::important 单选题9
`print(max('python+'))` 的运行结果是？（ ）（2分）

- A. 'p'
- B. p
- C. 'y'
- D. y [+print(max('python+'))的运行结果是？代码正确的是D]
:::
[+print(max('python+'))的运行结果是？代码正确的是D]:
    :::note 解析
    - `max()` 函数用于返回可迭代对象中的最大元素（按字符的 Unicode 编码值比较）。
    - `'python+'` 这几个字符的 Unicode 编码值分别为：'p'(112), 'y'(121), 't'(116), 'h'(104), 'o'(111), 'n'(110), '+'(43)。
    - 其中 'y' 的编码值最大。
    - `print(max('python+'))` 输出的是字符 y（不带引号）。
    - 所以，正确选项是 D。
    :::
:::important 单选题10
```python
a = 5.12596  
print(round(a, 2)) 
```
运行结果是？（ ）（2分）
- A. 5
- B. 5.1
- C. 5.12
- D. 5.13 [+a=5.12596print(round(a,2))运行结果是？，代码正确的是D]
:::
[+a=5.12596print(round(a,2))运行结果是？，代码正确的是D]:
    :::note 解析
    - round(a, 2) 表示将变量 a 四舍五入保留 2 位小数。
    - 5.12596 四舍五入到小数点后两位是 5.13。
    - 所以，正确选项是 D。
    :::
:::important 单选题11
`type([2.6])` 运行的结果是？（ ）（2分）

- A. float
- B. dict
- C. True
- D. list [+type()运行的结果是？代码正确的是D]
:::
[+type()运行的结果是？代码正确的是D]:
    :::note 解析
    - [2.6] 是一个包含一个浮点数元素的列表。
    - type([2.6]) 的结果是 <class 'list'>，即 list 类型。
    - 所以，正确选项是 D。
    :::
:::important 单选题12
执行如下代码  
```python
a = [1, 2, 3, 4]  
print(list(enumerate(a)))  
```
运行结果是？（ ）（2分）

- A. `((0, 1), (1, 2), (2, 3), (3, 4))`
- B. `[(0, 1), (1, 2), (2, 3), (3, 4)]` [+执行如下代码a=,print(list(enumerate(a)))运行结果是？，代码正确的是B]
- C. `[1, 2, 3, 4]`
- D. `(1, 2, 3, 4)`
:::
[+执行如下代码a=,print(list(enumerate(a)))运行结果是？，代码正确的是B]:
    :::note 解析
    - enumerate(a) 返回一个枚举对象，遍历时会生成 (index, value) 这样的元组。
    - list(enumerate(a)) 会将其转换为列表，结果为 [(0, 1), (1, 2), (2, 3), (3, 4)]。
    - 所以，正确选项是 B。
    :::
:::important 单选题13
set('hello') 运行结果是？（ ）（2分）

- A. `('h', 'e', 'l', 'l', 'o')`
- B. `{'h', 'e', 'l', 'l', 'o'}`
- C. `{'e', 'h', 'l', 'o'}`[+set('hello')运行结果是？代码正确的是C]
- D. `('e', 'h', 'l', 'o')`
:::
[+set('hello')运行结果是？代码正确的是C]:
    :::note 解析
    - set('hello') 会将字符串 'hello' 拆分为单个字符，并去除重复元素，结果是一个无序集合。
    - 集合中的元素不重复，且顺序不固定，通常显示为 {'e', 'h', 'l', 'o'}。
    - 所以，正确选项是 C。
    :::
:::important 单选题14
`print(sum([5, 10, min(7, 4, 6)]))`的运行结果是？（ ）（2分）

- A. 22
- B. 21 [+print(sum())的运行结果是？，代码正确的是B]
- C. 4
- D. 19
:::
[+print(sum())的运行结果是？，代码正确的是B]:
    :::note 解析
    - `min(7, 4, 6)` 返回最小值 `4`。
    - `sum([5, 10, 4])` 计算列表元素的总和：5 + 10 + 4 = 19。
    - 所以，正确选项是 B。
    :::
:::important 单选题15
`divmod(100, 3)` 的执行结果是？（ ）（2分）

- A. `(1, 33)` [+divmod(100,3)的执行结果是？，代码正确的是A]
- B. `(33, 1)`
- C. `[33, 1]`
- D. `[1, 33]`
:::
[+divmod(100,3)的执行结果是？，代码正确的是A]:
    :::note 解析
    - `divmod(100, 3)` 返回两个值：商和余数。
    - 商是 `100 // 3`，结果是 `33`。
    - 余数是 `100 % 3`，结果是 `1`。
    - 因此，`divmod(100, 3)` 的结果是 `(33, 1)`。
    - 所以，正确选项是 A。
    :::
:::important 单选题16
下列表达式结果是 False 的是？（ ）（2分）

- A. `all({})`
- B. `all([10])`
- C. `all(['1', '2', '3', ''])`
- D. `all(['1', '2', '3'])` [+下列表达式结果是False的是？，代码正确的是C]
:::
[+下列表达式结果是False的是？，代码正确的是C]:
    :::note 解析
    - `all()` 函数用于检查可迭代对象中的所有元素是否为真值。
    - **A. `all({})`**: 空集合 `{}` 被视为 `False`，但 `all()` 对空可迭代对象返回 `True`。
    - **B. `all([10])`**: 列表 `[10]` 中的元素 `10` 是 `True`。
    - **C. `all(['1', '2', '3', ''])`**: 列表中包含空字符串 `''`，它被视为 `False`，因此 `all()` 返回 `False`。
    - **D. `all(['1', '2', '3'])`**: 列表中的所有元素都是非空字符串，均为 `True`。
    - 所以，正确选项是 C。
    :::
:::important 单选题17
将字符串或数字转换为浮点数的函数是？（ ）（2分）

- A. chr()
- B. float() [+将字符串或数字转换为浮点数的函数是？，代码正确的是B]
- C. int()
- D. str()
:::
[+将字符串或数字转换为浮点数的函数是？，代码正确的是B]:
    :::note 解析
    - **A. `chr()`**: 用于将整数转换为其对应的 Unicode 字符。
    - **B. `float()`**: 用于将字符串或数字转换为浮点数。
    - **C. `int()`**: 用于将字符串或浮点数转换为整数。
    - **D. `str()`**: 用于将对象转换为字符串。
    - 因此，将字符串或数字转换为浮点数的函数是 `float()`。
    - 所以，正确选项是 B。
    :::
:::important 单选题18
将字符串或数字转换为浮点数的函数是？（ ）（2分）

- A. chr()
- B. float() [+将字符串或数字转换为浮点数的函数是？，代码正确的是B]
- C. int()
- D. str()
:::
[+将字符串或数字转换为浮点数的函数是？，代码正确的是B]:
    :::note 解析
    - **A. `chr()`**: 用于将整数转换为其对应的 Unicode 字符。
    - **B. `float()`**: 用于将字符串或数字转换为浮点数。
    - **C. `int()`**: 用于将字符串或浮点数转换为整数。
    - **D. `str()`**: 用于将对象转换为字符串。
    - 因此，将字符串或数字转换为浮点数的函数是 `float()`。
    - 所以，正确选项是 B。
    :::
:::important 单选题19
以下表达式的值为 True 是？（ ）（2分）

- A. bool(2022) [+以下表达式的值为True是？，代码正确的是A]
- B. bool(0)
- C. bool()
- D. bool({})
:::
[+以下表达式的值为True是？，代码正确的是A]:
    :::note 解析
    - **A. `bool(2022)`**: 非零整数被视为 `True`。
    - **B. `bool(0)`**: 零整数被视为 `False`。
    - **C. `bool()`**: 空值被视为 `False`。
    - **D. `bool({})`**: 空集合被视为 `False`。
    - 因此，表达式 `bool(2022)` 的值为 `True`。
    - 所以，正确选项是 A。
    :::

:::important 单选题20
有这样一段程序:(2分)
```python
a=["香蕉","苹果","草莓","哈密瓜"]
fs=open("fruits.csv","w")
fs.write(",".join(a)+"\n")
fs.close()
```

- A. 香蕉 苹果 草莓 哈密瓜
- B. 香蕉,苹果,草莓,哈密瓜[+有这样一段程序，代码正确的是B]
- C. 香蕉.苹果.草莓.哈密瓜 
- D. ["香蕉","苹果","草莓","哈密瓜"]
:::
[+有这样一段程序，代码正确的是B]:
    :::note 解析
    - 程序将列表 `a` 中的元素用 `.` 连接成一个字符串，并写入 `fruits.csv` 文件。
    - `fs.write(",".join(a)+"\n")` 将列表中的元素连接为 `'香蕉.苹果.草莓.哈密瓜'`，并在末尾添加换行符。
    - 因此，文件中的内容是 `'香蕉,苹果,草莓,哈密瓜'`。
    - 所以，正确选项是 B。
    :::
:::important 单选题21
关于下列表表，说法正确的是？（ ）（2分）
```python
s=[["佩奇", "100", "86", "85", "90"],
   ["苏西", "78", "88", "98", "89"],
   ["佩德罗", "80", "66", "80", "92"]]
```
- A. 这是一组二维数据 [+关于下列表表，说法正确的是？，代码正确的是A]
- B. 这样的数据不能存储到CSV文件中
- C. 必须手动写入到CSV文件中
:::
[+关于下列表表，说法正确的是？，代码正确的是A]:
    :::note 解析
    - **A. 这是一组二维数据**: 列表 `s` 是一个嵌套列表，包含多个子列表，每个子列表代表一行数据，因此是二维数据。
    - **B. 这样的数据不能存储到CSV文件中**: 二维数据可以通过循环和 `join()` 方法写入 CSV 文件。
    - **C. 必须手动写入到CSV文件中**: 可以通过编程自动写入 CSV 文件，而不需要手动操作。
    - 因此，正确的说法是 A。
    :::
:::important 单选题22
关于write()函数的说法正确的是？（ ）（2分）

- A. write()函数只能向文件中写入一行数据
- B. write()函数的参数不是字符串类型
- C. write()函数也可以向文件中写入多行数据 [+关于write()函数的说法正确的是？，代码正确的是C]
- D. write()函数和writelines()函数完全相同
:::
[+关于write()函数的说法正确的是？，代码正确的是C]:
    :::note 解析
    - **A. write()函数只能向文件中写入一行数据**: `write()` 可以写入任意长度的字符串，包括多行数据，只需在字符串中包含换行符。
    - **B. write()函数的参数不是字符串类型**: `write()` 的参数必须是字符串类型。
    - **C. write()函数也可以向文件中写入多行数据**: 通过在字符串中包含换行符 `\n`，可以写入多行数据。
    - **D. write()函数和writelines()函数完全相同**: `writelines()` 用于写入一个字符串列表，而 `write()` 用于写入单个字符串。
    - 因此，正确的说法是 C。
    :::
:::important 单选题23
对于在csv文件中追加数据，下列说法正确的是？（ ）（2分）

- A. 只能以单行方式追加数据
- B. 只能以多行方式追加数据
- C. 多行数据追加的函数是writerow()
- D. 以单行方式或多行方式追加都可以 [+对于在csv文件中追加数据，下列说法正确的是？，代码正确的是D]
:::
[+对于在csv文件中追加数据，下列说法正确的是？，代码正确的是D]:
    :::note 解析
    - **A. 只能以单行方式追加数据**: CSV 文件可以通过 `writerow()` 方法逐行追加数据。
    - **B. 只能以多行方式追加数据**: CSV 文件也可以通过 `writerows()` 方法追加多行数据。
    - **C. 多行数据追加的函数是writerow()**: `writerow()` 用于写入单行数据，`writerows()` 用于写入多行数据。
    - **D. 以单行方式或多行方式追加都可以**: 可以使用 `writerow()` 方法追加单行数据，也可以使用 `writerows()` 方法追加多行数据。
    - 因此，正确的说法是 D。
    :::
:::important 单选题24
Python的异常处理中try...except...else...finally机制中，以下哪部分语句一定能够得到执行？（ ）（2分）

- A. try子句
- B. except子句
- C. else子句
- D. finally子句 [+Python的异常处理中try...except...else...finally机制中，以下哪部分语句一定能够得到执行？，代码正确的是D]
:::
[+Python的异常处理中try...except...else...finally机制中，以下哪部分语句一定能够得到执行？，代码正确的是D]:
    :::note 解析
    - **A. try子句**: try子句中的代码只有在没有异常发生时才能完全执行。
    - **B. except子句**: except子句中的代码只有在try子句中发生异常时才会执行。
    - **C. else子句**: else子句中的代码只有在try子句中没有发生异常时才会执行。
    - **D. finally子句**: finally子句中的代码无论是否发生异常都会执行。
    - 因此，finally子句是唯一一个在try...except...else...finally结构中一定会执行的部分。
    - 所以，正确选项是 D。
    :::
:::important 单选题25
Python的异常处理机制中，以下表述哪项是错误的？（ ）（2分）

- A. 如果当try中的语句执行时发生异常，Python就执行匹配该异常的except子句。
- B. 如果当try中的语句执行时发生异常，try代码块的剩余语句将不会被执行。
- C. 如果在try子句执行时没有发生异常，Python将执行else语句后的语句。
- D. 异常处理结构能够发现程序段中的语法错误。 [+Python的异常处理机制中，以下表述哪项是错误的？，代码正确的是D]
:::
[+Python的异常处理机制中，以下表述哪项是错误的？，代码正确的是D]:
    :::note 解析
    - **A. 如果当try中的语句执行时发生异常，Python就执行匹配该异常的except子句。**: 这是正确的描述。
    - **B. 如果当try中的语句执行时发生异常，try代码块的剩余语句将不会被执行。**: 这是正确的描述。
    - **C. 如果在try子句执行时没有发生异常，Python将执行else语句后的语句。**: 这是正确的描述。
    - **D. 异常处理结构能够发现程序段中的语法错误。**: 异常处理结构用于处理运行时错误，而不是语法错误。语法错误会在代码执行前被检测到。
    - 因此，选项 D 是错误的。
    :::
### 2212判断题
:::important 判断题1
二进制数转化为十进制数的方法是：按权展开、逐项相加。如：(101)<sub>2</sub> = (10)<sub>10</sub>。（ ）（2分）

- 正确 [+二进制数转化为十进制数的方法是：按权展开、逐项相加。如：代码正确的是正确]
- 错误
:::
[+二进制数转化为十进制数的方法是：按权展开、逐项相加。如：代码正确的是正确]:
    :::note 解析
    - 二进制数转化为十进制数的方法是：将二进制数的每一位乘以对应的权值（2的幂次方），然后相加。
    - 例如，二进制数 `(101)<sub>2</sub>` 从右往左看：
        - 第0位是 `1`，权值是 2<sup>0</sup> = 1。值为 1 * 1 = 1。
        - 第1位是 `0`，权值是 2<sup>1</sup> = 2。值为 0 * 2 = 0。
        - 第2位是 `1`，权值是 2<sup>2</sup> = 4。值为 1 * 4 = 4。
    - 将各位的值相加：1 + 0 + 4 = 5。
    - 因此，二进制数 `(101)<sub>2</sub>` 对应的十进制数是 `5`。
    - 所以，正确选项是 正确。
    :::
:::important 判断题2
语句 `print(round(2.785, 2))` 运行后的结果是2.79。（ ）（2分）

- 正确
- 错误 [+语句print(round(2.785,2))运行后的结果是2.79。代码正确的是错误]
:::
[+语句print(round(2.785,2))运行后的结果是2.79。代码正确的是错误]:
    :::note 解析
    - Python 的 `round()` 函数用于将数字四舍五入到指定的小数位数。
    - 在 Python 中，`round(2.785, 2)` 的结果是 `2.78`，而不是 `2.79`。
    - 这是因为 `round()` 函数在处理 `.5` 的情况时，会将其圆整到最接近的偶数。
    - 因此，`2.785` 被四舍五入到 `2.78`。
    - 所以，正确选项是 错误。
    :::
:::important 判断题3
map() 不会根据提供的函数对指定序列做映射。（ ）（2分）

- 正确
- 错误 [+map()不会根据提供的函数对指定序列做映射。代码正确的是错误]
:::
[+map()不会根据提供的函数对指定序列做映射。代码正确的是错误]:
    :::note 解析
    - Python 中的 `map()` 函数正是用来根据提供的函数对指定序列做映射的。
    - `map()` 函数的语法是：`map(function, iterable, ...)`，它将 `function` 应用于 `iterable` 中的每个元素，并返回一个迭代器，其中包含应用函数后的结果。
    - 例如，`map(lambda x: x*2, [1, 2, 3])` 会返回一个迭代器，包含 `[2, 4, 6]`。
    - 因此，说 `map()` 不会根据提供的函数对指定序列做映射是错误的。
    - 所以，正确选项是 错误。
    :::
:::important 判断题4
语句 print(tuple(range(5)))的输出结果是(0, 1, 2, 3, 4)。（ ）（2分）

- 正确 [+语句print(tuple(range(5)))的输出结果是。代码正确的是正确]
- 错误
:::
[+语句print(tuple(range(5)))的输出结果是。代码正确的是正确]:
    :::note 解析
    - Python 中的 `range(5)` 函数会生成一个包含 0 到 4 的序列（不包括 5）。
    - `tuple()` 函数将这个序列转换为元组。
    - 因此，`tuple(range(5))` 的结果是元组 `(0, 1, 2, 3, 4)`。
    - 当使用 `print()` 函数打印这个元组时，输出就是 `(0, 1, 2, 3, 4)`。
    - 所以，正确选项是 正确。
    :::
:::important 判断题5
运行语句 set('2022')，其输出结果是{'2','0','2','2'}。（ ）（2分）

- 正确
- 错误 [+运行语句set('2022')，其输出结果是{'2','0','2','2'}。代码正确的是错误]
:::
[+运行语句set('2022')，其输出结果是{'2','0','2','2'}。代码正确的是错误]:
    :::note 解析
    - Python 中的 `set()` 函数用于创建一个集合（set）。
    - 集合是一个无序且不重复的元素集。
    - 当我们执行 `set('2022')` 时，它会将字符串 '2022' 转换为一个集合，其中包含字符串中的所有唯一字符。
    - 由于字符 '2' 在字符串中出现了两次，但在集合中每个元素只能出现一次，所以结果集合中字符 '2' 只会出现一次。
    - 因此，`set('2022')` 的结果是 `{'2', '0'}`，而不是 `{'2', '0', '2', '2'}`。
    - 所以，正确选项是 错误。
    :::
:::important 判断题6
以下代码能正确读取CSV文件：
```python
f=open('ss.csv','r')
n=f.read().strip("\n").split(",")
f.close()
```
（ ）（2分）

- 正确 [+以下代码能正确读取CSV文件。代码正确的是正确]
- 错误
:::
[+以下代码能正确读取CSV文件。代码正确的是正确]:
    :::note 解析
    - 这段代码打开了名为 'ss.csv' 的CSV文件，并以读取模式('r')读取其内容。
    - `f.read()` 读取整个文件内容作为一个字符串。
    - `.strip("\n")` 移除字符串首尾的换行符。
    - `.split(",")` 按逗号分割字符串，生成一个列表。
    - 这种方法适用于简单的单行CSV文件，但对于多行CSV文件，这种处理方式不够完善，因为它会将所有行合并处理，而不是按行分别处理。
    - 对于多行CSV文件，更合适的处理方式是先按行分割，然后对每行内容按逗号分割。
    - 但从技术上讲，这段代码确实能够读取CSV文件并提取其中的数据，尽管它可能不是处理多行CSV文件的最佳方法。
    - 所以，该代码能够正确读取CSV文件，判断为正确。
    :::
:::important 判断题7
一维数组可以用列表实现，二维数组则不能用列表实现。（ ）（2分）

- 错误 [+一维数组可以用列表实现，二维数组则不能用列表实现。代码正确的是错误]
:::
[+一维数组可以用列表实现，二维数组则不能用列表实现。代码正确的是错误]:
    :::note 解析
    - Python 中的列表（list）是一种非常灵活的数据结构，可以用来实现各种维度的数组。
    - 一维数组可以直接用 Python 列表表示，例如：`[1, 2, 3, 4, 5]`。
    - 二维数组可以用嵌套列表（列表的列表）来实现，例如：`[[1, 2, 3], [4, 5, 6], [7, 8, 9]]`，这表示一个 3×3 的二维数组。
    - 同样，三维及更高维度的数组也可以通过进一步嵌套列表来实现。
    - 因此，说"二维数组则不能用列表实现"是错误的。
    - 所以，正确选项是 错误。
    :::
:::important 判断题8
用with open ('fruits.csv','r')as f语句，打开fruits.csv文件，在处理结束后不会自动关闭文件。（ ）（2分）

- 错误 [+用withopen语句，打开fruits.csv文件，在处理结束后不会自动关闭文件。代码正确的是错误]
:::
[+用withopen语句，打开fruits.csv文件，在处理结束后不会自动关闭文件。代码正确的是错误]:
    :::note 解析
    - Python 中的 `with` 语句是一种上下文管理器（context manager），它能够自动处理资源的获取和释放。
    - 当使用 `with open(...) as f` 语句打开文件时，Python 会在 `with` 代码块执行完毕后自动关闭文件，无论代码块是正常结束还是因为异常而结束。
    - 这是 `with` 语句的主要优点之一，它确保了文件等资源在不再需要时被正确关闭，避免了资源泄漏。
    - 相比于传统的 `f = open(...); try: ... finally: f.close()` 模式，`with` 语句更加简洁且不易出错。
    - 因此，说"在处理结束后不会自动关闭文件"是错误的。
    - 所以，正确选项是 错误。
    :::
:::important 判断题9
异常处理结构中，finally程序段中的语句不一定都会得到执行。（ ）（2分）

- 错误 [+异常处理结构中，finally程序段中的语句不一定都会得到执行。代码正确的是错误]
:::
[+异常处理结构中，finally程序段中的语句不一定都会得到执行。代码正确的是错误]:
    :::note 解析
    - 在 Python 的异常处理结构中，`finally` 语句块中的代码无论是否发生异常，通常都会被执行。
    - 即使在 `try` 块或 `except` 块中发生了异常，或者执行了 `return`、`break`、`continue` 等跳转语句，`finally` 块中的代码仍然会被执行。
    - 只有极少数情况下（如程序进程被强制终止、操作系统崩溃等），`finally` 语句块才不会被执行。
    - 因此，通常可以认为 `finally` 语句块中的代码一定会被执行。
    - 所以，正确选项是 错误。
    :::
:::important 判断题10
在计算机中，每一个二进制位可以表示0和1两种信息。（ ）（2分）

- 正确 [+在计算机中，每一个二进制位可以表示0和1两种信息。代码正确的是正确]
:::
[+在计算机中，每一个二进制位可以表示0和1两种信息。代码正确的是正确]:
    :::note 解析
    - 计算机采用二进制系统，每一个二进制位（bit）只能表示两种状态：0 或 1。
    - 这两种状态可以用来表示两种不同的信息，例如：开/关、真/假、是/否等。
    - 多个二进制位组合在一起可以表示更多的信息。
    - 所以，正确选项是 正确。
    :::
### 2212编程题
::::important 编程题1. 补全代码(10分)
下面程序实现对二维数据的处理，请你补全代码。

score2.CSV 文件内容如下：
```
王鑫,86,90,92,99,94,95
杨小虹,93,97,95,90,86,85
李静,89,98,99,94,90,85
```

:::code-tabs
@tab Python 
```python :collapsed-lines=5
f=open('/data/score2.csv','r')
a=[]
for i in f:
    a.append(i.strip().split(','))  # 读取每一行并按逗号分割，形成二维列表
f.close()
print(a)  # ① 输出二维列表，便于检查数据结构
for i in a:
    s=""
    for j in i:
        s += j + "\t"  # ② 将每个元素用制表符拼接，便于格式化输出
    print(s)
```
:::
- 解析[+下面程序实现对二维数据的处理，请你补全代码解析]
[+下面程序实现对二维数据的处理，请你补全代码解析]:
    :::note 解析
    - ① 处应填写 `print(a)`，用于输出二维列表，检查数据结构是否正确。
    - ② 处应填写 `s += j + "\t"`，将每个元素用制表符拼接，便于格式化输出。
    - 程序首先读取 CSV 文件的每一行，并用 `split(',')` 分割成列表，最终形成一个嵌套列表（二维列表）。
    - 外层循环遍历每一行（即每个学生的数据），内层循环遍历该行的每个元素（姓名和成绩），用制表符拼接后输出，实现表格化显示。
    :::
::::
     
::::important 编程题2. 统计整数的个数(10分)
在三位数的自然数中，找出至少有一位数字是5的，且至少能被3整除的所有整数，并统计个数，具体代码如下：
:::code-tabs
@tab Python
```python :collapsed-lines=5
count=0
lst=[]
for i in range(100,1000):  # ① 三位数范围是100~999
    if i%3==0:  # 能被3整除
        a=i%10  # 个位
        b=i//10%10  # 十位
        c=i//100  # ② 百位
        if a==5 or b==5 or c==5:  # ③ 判断任意一位是否为5
            count+=1
            lst.append(i)
print("这样的三位数有：",lst)
print("总数量有：",count)
```
:::
- 解析[+在三位数的自然数中，找出至少有一位数字是5的，且至少能被3整除的所有整数，并统计个数解析]
[+在三位数的自然数中，找出至少有一位数字是5的，且至少能被3整除的所有整数，并统计个数解析]:
    :::note 解析
    - ① 处应填写 `100,1000`，表示三位数的范围（100~999）。三位数的自然数从100到999，range(100,1000)可以遍历所有三位数。
    - ② 处应填写 `i//100`，用于获取百位数字。`i%10` 得到个位，`i//10%10` 得到十位，`i//100` 得到百位。
    - ③ 处应填写 `a==5 or b==5 or c==5`，判断任意一位是否为5。只要个位、十位或百位有一个等于5即可。
    - 程序思路：先遍历所有三位数，判断能否被3整除，再判断是否有一位为5，符合条件则统计并保存到列表。
    - 这样可以确保所有符合条件的三位数都被统计到，且输出所有结果和总数量。
    :::
::::



          
::::important 3. 求平方根(10分)
输入一个正数，以下代码编程求出它的平方根。请你补全代码。
:::code-tabs
@tab Python
```python :collapsed-lines=5
in_var = float(input("请输入一个需要开方的正数，可以使用2位小数！\nx = "))
if in_var < 0:
    x = -in_var
else:
    x = in_var
low = 0.0
high = x
s_root = (low + high) / 2  # ① 初始猜测值为区间中点
if x > 0 and x < 1:
    high = 1.0
    low = 0
    s_root = (low + high) / 2  # ② 针对0~1区间重新赋初值
if x >= 0:
    while abs(s_root ** 2 - x) > 0.0001:  # ③ 判断平方与目标值的误差
        if x > 1.0:
            if s_root ** 2 < x:
                low = s_root
            else:
                high = s_root
            s_root = (low + high) / 2  # ④ 二分法更新猜测值
        elif x == 1.0 and x == 0.0:
            s_root = x
        else:
            if s_root ** 2 < x:
                low = s_root
            else:
                high = s_root
            s_root = (low + high) / 2  # ⑤ 二分法更新猜测值
if in_var >= 0:
    print("所求数的平方根为：s_root = %.1f"%(s_root))
```
:::
- 解析[+输入一个正数，以下代码编程求出它的平方根。请你补全代码解析]
[+输入一个正数，以下代码编程求出它的平方根。请你补全代码解析]:
    :::note 解析
    - ①、②、④、⑤ 处均应填写 `(low + high) / 2`，即二分法的中点作为当前猜测的平方根。
    - ③ 处应填写 `s_root ** 2 - x`，即当前猜测平方与目标值的差的绝对值。
    - 程序采用二分法逼近平方根，适用于任意正数，误差小于0.0001时停止迭代。
    - 对于0~1区间和大于1的数分别处理，保证二分区间正确。
    - 最终输出保留一位小数的平方根值。
    :::
::::

        