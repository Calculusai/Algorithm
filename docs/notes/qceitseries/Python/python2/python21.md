---
title: Python2级试卷
createTime: 2025/03/21 12:33:09
icon: tabler:rosette-number-2
permalink: /qceitseries/Python/python2/python21.html
---
## 试卷2209
### 2209单选题
:::important 单选题1
这道题考察的是 Python 中列表（List）和 `len()` 函数的基本使用：
1. 代码内容：
```python
means=['Thank','You']
print(len(means))
```
2. 考点分析：
- 列表的创建和基本语法
- `len()` 函数的使用 - 用于获取序列（如列表、字符串等）的长度
- Python 中字符串作为列表元素的使用
3. 具体分析：
- `means` 是一个包含两个字符串元素的列表：`'Thank'` 和 `'You'`
- `len(means)` 会返回列表 `means` 中元素的个数
- 因此输出结果为 `2`
4. 答案解释：
- 选项 C（2）是正确答案
- 因为列表 `means` 只包含两个元素：`'Thank'` 和 `'You'`
- `len()` 函数返回的是列表中元素的数量，而不是字符串的总长度
这是一道基础的 Python 列表操作题目，主要测试考生是否理解列表的基本概念和 `len()` 函数的使用方法。
:::
### 2209判断题
### 2209编程题
::::important 行李尺寸检查
```python :collapsed-lines=5
while True:
    # 获取输入并分割
    dimensions = input("请输入行李的长、宽、高(用逗号分隔): ").split(',')
    l, w, h = map(int, dimensions)
        
    # 判断是否满足条件
    if (60 <= l <= 203) and (60 <= w <= 203) and (60 <= h <= 203):
        print("可以托运")
    else:
        print("不可以托运")
```
::::
::::important 编写程序统计文本中的话
```python :collapsed-lines=5
# 获取用户输入的文本
text = input("请输入一段文字：")

# 初始化计数器
count = 0

# 遍历文本中的每个字符
for char in text:
    # 判断是否是句子结束符号
    if char in '.?!':
        count += 1

# 输出统计结果
print(f"这段话中共有{count}句话")
```
::::
## 试卷2212
### 2212单选题
:::important 单选题1
1.  运行下列程序，最终输出的结果是？（ ）
```python
info = {1:'小明', 2:'小黄', 3:'小兰'}
info[4] = '小红'
info[2] = '小白'
print(info)
```
- A. ```{1: '小明', 2: '小白', 3: '小红', 4: '小兰'}```
- B. ```{1: '小明', 2: '小白', 3: '小兰', 4: '小红'}```[+运行下列程序，最终输出的结果是B]
- C. ```{1: '小白', 2: '小名', 3: '小兰', 4: '小红'}```
- D. ```{1: '小明', 2: '小红', 3: '小兰', 4: '小白'}```
:::
[+运行下列程序，最终输出的结果是B]:
    :::note 解析
    - info = {1:'小明', 2:'小黄', 3:'小兰'} ：首先，创建了一个字典 info ，包含三个键值对。
    - info[4] = '小红' ：向字典 info 中添加一个新的键值对。因为键 4 不存在，所以会新增。此时字典变为 {1: '小明', 2: '小黄', 3: '小兰', 4: '小红'} 。
    - info[2] = '小白' ：修改字典 info 中键为 2 的值。因为键 2 已经存在，所以会覆盖原来的值 '小黄' 。此时字典变为 {1: '小明', 2: '小白', 3: '小兰', 4: '小红'} 。
    - print(info) ：打印最终的字典 info 。
    - 因此，最终输出的结果是 {1: '小明', 2: '小白', 3: '小兰', 4: '小红'} ，对应选项 B。
    :::
:::important 单选题2
```{1: '小明', 2: '小红', 3: '小兰', 4: '小白'}```
```python
s=0
k=1
for i in range(1,101):
    s=s+k*i
    k=-k
print(s)
```
- A. s=1+2+3+5+6...+100的值
- B. s=1-2+3-4+5-6+...-100的值[+这个自己如下代码后的结果是B]
- C. s=1+2+3+4+5+6...+101的值
- D. s=1-2+3-4-5-6-...-101的值
:::
[+这个自己如下代码后的结果是B]:
    :::note 解析
    - 首先，我们需要了解一下题目中所使用的变量：
        - s ：表示累加的结果，初始值为 0。
        - k ：表示当前项的符号，初始值为 1。
        - i ：表示当前项的序号，从 1 到 100。
    - 接下来，我们来分析一下程序的执行过程：
        - 当 i=1 时，s=s+k* i ，即 s=0+1*1=1 。
        - 当 i=2 时，k=-k ，即 k=-1 。然后，s=s+k * i ，即 s=1-1 * 2=-1 。
        - 当 i=3 时，k=-k ，即 k=1 。然后，s=s+k * i ，即 s=-1+1 * 3=2 。
        - 当 i=4 时，k=-k ，即 k=-1 。然后，s=s+k * i ，即 s=2-1 * 4=-2 。
        - 以此类推，直到 i=100 时，k=-k ，即 k=1 。然后，s=s+k * i ，即 s=-100+1*100=100 。
    - 最终，s 的值为 100 ，对应选项 B。
    :::

:::important 单选题3
有以下三个词组的列表，
```python
game_name=['一只蜗牛','一只猫','一只兔子','孩子们','运动员们','一朵云']
game_where=['在伞底下','在沙滩上','在沙发上','在石头上','在天空中','在公园里']
game_what=['搭了个窝','在慢慢爬','玩滑梯','打排球','正在睡觉','遮住太阳']
```
如果造句的输出结果是“一只兔子在公园里搭了个窝”，代码正确的是？（ ）(2分)

- A. `print(game_name[-4]+game_where[-1]+game_what[0])`[+如果造句的输出结果是一只兔子在公园里搭了个窝，代码正确的是A]
- B. `print(game_name[2]+game_where[6]+game_what[0])`
- C. `print(game_name[2]+game_where[6]+game_what[0])`
- D. `print(game_name[-4]+game_where[5]+game_what[1])`
:::
[+如果造句的输出结果是一只兔子在公园里搭了个窝，代码正确的是A]:
    :::note 解析
    - `game_name[-4]` 表示列表 game_name 中倒数第四个元素，即 '一只蜗牛' 。
    - `game_where[-1]` 表示列表 game_where 中倒数第一个元素，即 '在公园里' 。
    - `g`ame_what[0]` 表示列表 game_what 中第一个元素，即 '搭了个窝' 。
    - 因此，代码 `print(game_name[-4]+game_where[-1]+game_what[0])` 的输出结果为 '一只蜗牛在公园里搭了个窝' ，对应选项 A。
    :::

:::important 单选题4
执行下面代码，输出的结果是？（ ）
```python
for leibie in 'student':
    if leibie=='u':
        break
    print(leibie)
```
(2分)

- A. [+执行下面代码，输出的结果是A]
```python
st
```

- B. 
```python
stu
```
- C. 
```python
s
t
s
```
- D.
```python
t
u
```
:::
[+执行下面代码，输出的结果是A]:
    :::note 解析
    - 首先，我们需要了解一下题目中所使用的变量：
        - leibie ：表示当前遍历到的字符。
        - 'student' ：表示要遍历的字符串。
    - 接下来，我们来分析一下程序的执行过程：
        - 当 leibie='s' 时，由于没有遇到字符 'u' ，所以不会执行 `break` 语句。此时，会执行 `print(leibie)` ，输出 's' 。    
        - 当 leibie='t' 时，由于没有遇到字符 'u' ，所以不会执行 `break` 语句。此时，会执行 `print(leibie)` ，输出't' 。
        - 当 leibie='u' 时，由于遇到了字符 'u' ，所以会执行 `break` 语句。此时，程序会跳出循环，不会再执行循环。

    - 最终，程序会输出 's' 、 't' 对应选项 A。
    :::
:::important 单选题5
`score={'跳绳':85,'跳远':99,'跑步':92}`。下列哪个代码可以将跳绳的85修改成95？（ ）(2分)

- A. `score[跳绳]=95`
- B. `score['跳绳']=95`[+下列哪个代码可以将跳绳的85修改成95，代码正确的是A]
- C. `score[0]=95`
- D. `score['0']=95`
:::
[+下列哪个代码可以将跳绳的85修改成95，代码正确的是A]:
    :::note 解析
    - 字典的键是不可变的，因此不能使用数字作为键。
    - 字典的键必须是唯一的，因此不能使用相同的键来存储不同的值。
    - 字典的值可以是任意类型的，因此可以使用不同的键来存储不同类型的值。
    - 字典的键必须是可哈希的，因此不能使用可变类型（如列表）作为键。
    - 其中`'跳绳'`是字典的键，而`85`是字典的值。
    - 因此，`score['跳绳']=95`是正确的代码，可以将`'跳绳'`对应的值修改为`95
    :::

:::important 单选题6
已知列表`a=[2,4,6]`，执行语句`print(2*a)`，显示的结果是？（ ）(2分)

- A. `[[2, 4, 6],[2, 4, 6]]`
- B. `[2, 4, 6, 2, 4, 6]`[+已知列表，执行语句显示的结果是B]
- C. `[4, 16, 36]`
- D. `[4, 8, 12]`
:::
[+已知列表，执行语句显示的结果是B]:
    :::note 解析
    - 列表的乘法操作是将列表中的元素重复多次，然后将重复后的元素组成一个新的列表。
    - 因此，`print(2*a)`的结果是`[2, 4, 6, 2, 4, 6]`，对应选项 B。
    :::

:::important 单选题7
下列程序执行后，打印的结果是？（ ）(2分)[+下列程序执行后，打印的结果是？，代码正确的是D]
```python
a=[3,9,5]
b=a
b[0]=7
print(b)
```
- A. `[3,9,5]`
- B. `[7,3,9,5]`
- C. `[3,9,5,7]`
- D. `[7,9,5]`
:::
[+下列程序执行后，打印的结果是？，代码正确的是D]:
    :::note 解析
    - 首先，我们需要了解一下题目中所使用的变量：
        - a ：表示列表 `[3, 9, 5]` 。
        - b ：表示列表 `[3, 9, 5]` 。
    - 接下来，我们来分析一下程序的执行过程：
        - 当 `b=a` 时，将列表 a 的引用赋值给变量 b 。此时，变量 a 和变量 b 都指向同一个列表。
        - 当 `b[0]=7` 时，将列表 b 中索引为 0 的元素修改为 7 。此时，列表 a 和列表 b 都被修改了。
        - 当 `print(b)` 时，打印列表 b 。此时，列表 b 的值为 `[7, 9, 5]` 。
    - 最终，程序会输出 `[7, 9, 5]` ，对应选项 D。
    :::

:::important 单选题8
```python
toy=['魔方','拼图']
toy.append('积木')
print(toy)
```
以上代码的运行结果是？（ ）(2分)
- A. `['魔方','拼图',['积木']]`
- B. `['魔方','拼图','积木']`[+以上代码的运行结果是？，代码正确的是B]
- C. `['积木','魔方','拼图']`
- D. `'魔方','拼图','积木'`
:::
[+以上代码的运行结果是？，代码正确的是B]:
    :::note 解析
    - 首先，我们需要了解一下题目中所使用的变量：
        - toy ：表示列表 `['魔方', '拼图']` 。
    - 接下来，我们来分析一下程序的执行过程：
        - 当 `toy.append('积木')` 时，将字符串 '积木' 添加到列表 toy 的末尾。此时，列表 toy 的值为 `['魔方', '拼图', '积木']` 。
        - 当 `print(toy)` 时，打印列表 toy 。此时，列表 toy 的值为 `['魔方', '拼图', '积木']` 。
    - 最终，程序会输出 `['魔方', '拼图', '积木']` ，对应选项 B。
    :::

:::important 单选题9
```python
a=1
for i in range(4):
    if i==3:
        break
        a+=1
    else:
        a+=2
print(a)
```
以上代码的运行结果是？（ ）(2分)

- A. 4
- B. 5
- C. 6
- D. 7[+以上代码的运行结果是？，代码正确的是D]
:::
[+以上代码的运行结果是？，代码正确的是D]:
    :::note 解析
    - 首先，我们需要了解一下题目中所使用的变量：
        - a ：表示变量 a 的值，初始值为 1 。
        - i ：表示变量 i 的值，从 0 到 3 。
    - 接下来，我们来分析一下程序的执行过程：
        - 当 i=0 时，由于 i 不等于 3 ，所以不会执行 `break` 语句。此时，a=a+2 ，即 a=1+2=3 。
        - 当 i=1 时，由于 i 不等于 3 ，所以不会执行 `break` 语句。此时，a=a+2 ，即 a=3+2=5 。
        - 当 i=2 时，由于 i 不等于 3 ，所以不会执行 `break` 语句。此时，a=a+2 ，即 a=5+2=7 。
        - 当 i=3 时，由于 i 等于 3 ，所以会执行 `break` 语句。此时，程序会跳出循环，不会再执行循环。
    - 最终，程序会输出 7 ，对应选项 D。
    :::
好的，我已经查看了图片。图片内容是一个关于 Python 字典删除操作的选择题。

这是图片内容的 Markdown 格式：


:::important 单选题10
想要删除gift字典中的键值对 '糖果': 10, 可以使用的代码是？（ ）(2分)

- A. `gift.pop('糖果':10)`
- B. `gift.pop('糖果')`[+可以使用的代码是？，代码正确的是B]
- C. `gift.remove('糖果':10)`
- D. `gift.remove('糖果')`
:::
[+可以使用的代码是？，代码正确的是B]:
    :::note 解析
    - 字典的 `pop()` 方法可以根据键来删除键值对。
    - 字典中没有 `remove()` 方法，只能使用 `pop()` 和 `del` 关键字来删除元素。
    - 因此，`gift.pop('糖果')` 是正确的代码，可以删除字典 gift 中的键值对 `'糖果': 10` ，对应选项 B。
    :::

:::important 单选题11
在空格处填入以下哪个语句，==不能==得到从大到小的排序结果是？（ ）(2分)
```python
list1 =[1,5,9,67,4,44,99]
for i in range(2):
    a = int(input())
    list1.append(a)
_________________ # 空格处
for i in list1:
    print(i,end = " ")
```

- A. `list1.sort()`[+在空格处填入以下哪个语句，不能得到从大到小的排序结果是？，代码正确的是A]
- B. `list1 = sorted(list1,reverse=True)`
- C. `list1 = sorted(list1,reverse=True)`
- D. `list1 = sorted(list1,reverse=True)`
:::
[+在空格处填入以下哪个语句，不能得到从大到小的排序结果是？，代码正确的是A]:
    :::note 解析
    - 首先，我们需要了解一下题目中所使用的变量：
        - list1 ：表示列表 `[1, 5, 9, 67, 4, 44, 99]` 。
        - a ：表示用户输入的数字。
        - i ：表示循环变量。
    - 接下来，我们来分析一下程序的执行过程：
        - 当 `for i in range(2):` 时，循环执行两次。
        - 当 i=0 时，用户输入一个数字，然后将该数字添加到列表 list1 的末尾。
        - 当 i=1 时，用户输入一个数字，然后将该数字添加到列表 list1 的末尾。
        - 当 `list1.sort()` 时，将列表 list1 中的元素从小到大排序。
        - 当 `for i in list1:` 时，循环遍历列表 list1 中的元素。
        - 当 `print(i, end=" ")` 时，打印列表 list1 中的元素，每个元素之间用空格分隔。
    - `reverse` 参数用于指定排序的顺序,默认是`False`。如果 `reverse=True` ，则从大到小排序；如果 `reverse=False` ，则从小到大排序。
    - 最终，程序会输出列表 list1 中的元素，从大到小排序,对应选项 A。
    :::

:::important 单选题12
下列关于列表变量ls操作的描述中，错误的是？（ ）(2分)

- A. `min(ls)`:找出ls中最小元素
- B. `ls.remove(x)`:删除ls中所有x元素 [+下列关于列表变量ls操作的描述中，错误的是？，代码正确的是B]
- C. `ls.index(x)`:返回元素x在列表中的下标
- D. `ls.index(x)`:返回元素x在列表中的下标
:::
[+下列关于列表变量ls操作的描述中，错误的是？，代码正确的是B]:
    :::note 解析
    - `min(ls)`: 正确，这是 Python 的内置函数，用于找出可迭代对象（如列表 `ls`）中的最小元素。
    - `ls.remove(x)`: **错误**。列表的 `remove(x)` 方法只会删除列表中遇到的第一个值为 `x` 的元素。如果列表中有多个 `x`，它不会删除所有的 `x`。
    - `ls.index(x)`: 正确，列表的 `index(x)` 方法用于返回列表中第一个值为 `x` 的元素的索引（下标）。如果 `x` 不在列表中，会引发 `ValueError`。选项 C 和 D 描述相同且正确。
    - 因此，描述错误的是选项 B。
    :::

:::important 单选题13
下面代码的输出结果是？（ ）(2分)
```python
name = "Python语言程序设计"
for i in range(5):
    print(name[i],end='')
```
- A. Python
- B. Pytho [+下面代码的输出结果是？，代码正确的是B]
- C. ython
- D. Python语言程序
:::
[+下面代码的输出结果是？，代码正确的是B]:
    :::note 解析
    - `name = "Python语言程序设计"`: 定义了一个字符串变量 `name`。
    - `for i in range(5):`: 循环将遍历 `i` 从 0 到 4 (不包括 5)。
    - `print(name[i], end='')`: 在每次循环中，打印字符串 `name` 中索引为 `i` 的字符。`end=''` 参数表示打印后不换行，而是打印一个空字符串。
    - 循环过程：
        - i=0: 打印 `name[0]` 即 'P'
        - i=1: 打印 `name[1]` 即 'y'
        - i=2: 打印 `name[2]` 即 't'
        - i=3: 打印 `name[3]` 即 'h'
        - i=4: 打印 `name[4]` 即 'o'
    - 由于 `end=''`，所有字符会连续打印在同一行。
    - 因此，最终输出结果是 "Pytho"，对应选项 B。
    :::

:::important 单选题14
下列程序的运行结果是？（ ）
```python
str1="cat dog cat dog cat dog"
str2=str1.replace("cat","mouse",2)
print(str2)
```
(2分)

- A. `cat dog mouse dog cat dog`
- B. `2 dog mouse dog cat dog`
- C. `mouse mouse dog mouse dog cat dog`
- D. `mouse dog mouse dog cat dog` [+下列程序的运行结果是？，代码正确的是D]
:::
[+下列程序的运行结果是？，代码正确的是D]:
    :::note 解析
    - `str1="cat dog cat dog cat dog"`: 定义了一个字符串变量 `str1`。
    - `str2=str1.replace("cat","mouse",2)`: 使用 `replace()` 方法替换 `str1` 中的子字符串。
        - `"cat"` 是要被替换的子字符串。
        - `"mouse"` 是用来替换的新子字符串。
        - `2` 是可选的第三个参数，表示最多替换的次数。这里表示最多替换前 2 个 "cat"。
    - 替换过程：
        - 第一个 "cat" 被替换为 "mouse"，字符串变为 `"mouse dog cat dog cat dog"`。
        - 第二个 "cat" 被替换为 "mouse"，字符串变为 `"mouse dog mouse dog cat dog"`。
        - 由于指定了最多替换 2 次，第三个 "cat" 不会被替换。
    - `print(str2)`: 打印变量 `str2` 的最终值。
    - 因此，最终输出结果是 `"mouse dog mouse dog cat dog"`，对应选项 D。
    :::

:::important 单选题15
下列程序的运行结果是？（ ）
```python
tem=(4,7,4,6,9)
tem=tem+(4,)
print(tem.count(4))
```
(2分)

- A. 2
- B. 0
- C. 程序报错
- D. 3 [+下列程序的运行结果是？，代码正确的是D]
:::
[+下列程序的运行结果是？，代码正确的是D]:
    :::note 解析
    - `tem=(4,7,4,6,9)`: 初始化一个元组 `tem`，其值为 `(4, 7, 4, 6, 9)`。
    - `tem=tem+(4,)`: 将元组 `tem` 与另一个只包含元素 `4` 的元组 `(4,)` 进行连接。注意 `(4,)` 是一个单元素元组，逗号是必需的。连接后的新元组为 `(4, 7, 4, 6, 9, 4)`，并将这个新元组重新赋值给变量 `tem`。
    - `print(tem.count(4))`: 调用元组 `tem` 的 `count()` 方法，统计元素 `4` 在元组中出现的次数。在元组 `(4, 7, 4, 6, 9, 4)` 中，元素 `4` 出现了 3 次。
    - 因此，最终输出结果是 `3`，对应选项 D。
    :::

:::important 单选题16
下列说法正确的是？（ ）(2分)

- A. if语句通常用于重复执行某一段程序
- B. while语句通常用于执行条件判断
- C. 在Python中可以使用do..while循环
- D. break语句可以跳出for和while的循环体 [+下列说法正确的是？，代码正确的是D]
:::
[+下列说法正确的是？，代码正确的是D]:
    :::note 解析
    - A. **错误**。`if` 语句用于根据条件执行不同的代码块，而不是重复执行。重复执行通常使用 `for` 或 `while` 循环。
    - B. **错误**。`while` 语句是循环语句，它根据条件重复执行代码块。虽然它包含条件判断，但其主要目的是循环，而不是像 `if` 那样只做一次条件判断。
    - C. **错误**。Python 语言没有 `do..while` 循环结构。`while` 循环会在执行循环体之前检查条件。
    - D. **正确**。`break` 语句用于立即终止并跳出其所在的当前 `for` 或 `while` 循环。
    - 因此，正确的说法是选项 D。
    :::

:::important 单选题17
已知有字符串str2="Good Morning!", 则执行print(len(str2[1:-2]))输出的结果是？（ ）(2分)

- A. 8
- B. 9
- C. 10 [+已知有字符串,则执行输出的结果是？，代码正确的是C]
- D. 11
:::
[+已知有字符串,则执行输出的结果是？，代码正确的是C]:
    :::note 解析
    - `str2 = "Good Morning!"`: 定义了一个字符串变量 `str2`。
    - `str2[1:-2]`: 对字符串 `str2` 进行切片操作。
        - `1` 是起始索引（包含），对应字符 'o'。
        - `-2` 是结束索引（不包含）。负数索引从右往左数，-1 是最后一个字符 '!', -2 是倒数第二个字符 'g'。所以切片到 'g' 之前结束。
        - 切片的结果是 `"ood Mornin"`。
    - `len(str2[1:-2])`: 计算切片后字符串 `"ood Mornin"` 的长度。
    - `"ood Mornin"` 包含 10 个字符。
    - `print(...)`: 输出计算得到的长度。
    - 因此，最终输出结果是 `10`，对应选项 C。
    :::

:::important 单选题18
运行下列程序段后，输出的结果是？（ ）
```python
i=1
while i<=5:
    i=i+2
print(i)
```
(2分)

- A. 3
- B. 7 [+运行下列程序段后，输出的结果是？，代码正确的是B]
- C. 5
- D. 6
:::
[+运行下列程序段后，输出的结果是？，代码正确的是B]:
    :::note 解析
    - `i=1`: 初始化变量 `i` 为 1。
    - `while i<=5:`: 进入 `while` 循环，条件是 `i` 小于或等于 5。
    - **第一次循环**: `i` (1) <= 5 为 True。执行 `i = i + 2`，`i` 变为 3。
    - **第二次循环**: `i` (3) <= 5 为 True。执行 `i = i + 2`，`i` 变为 5。
    - **第三次循环**: `i` (5) <= 5 为 True。执行 `i = i + 2`，`i` 变为 7。
    - **第四次循环**: `i` (7) <= 5 为 False。循环终止。
    - `print(i)`: 打印循环结束后 `i` 的值，此时 `i` 为 7。
    - 因此，最终输出结果是 `7`，对应选项 B。
    :::

:::important 单选题19
运行语句`print("ab"<"ac")`后，输出的结果是？（ ）(2分)

- A. 0
- B. 1
- C. True [+运行语句print()后，输出的结果是？，代码正确的是C]
- D. False
:::
[+运行语句print()后，输出的结果是？，代码正确的是C]:
    :::note 解析
    - `print("ab"<"ac")`: 这行代码比较两个字符串 `"ab"` 和 `"ac"` 的大小。
    - Python 字符串比较是按字典序（lexicographical order）进行的，逐个比较字符的 ASCII (或 Unicode) 值。
    - 比较第一个字符：'a' 和 'a' 相等。
    - 比较第二个字符：'b' 和 'c'。因为 'b' 的 ASCII 值小于 'c' 的 ASCII 值，所以 `"ab"` 小于 `"ac"`。
    - 因此，表达式 `"ab" < "ac"` 的结果是布尔值 `True`。
    - `print()` 函数会输出这个布尔值。
    - 最终输出结果是 `True`，对应选项 C。
    :::

:::important 单选题20
若a=[1,2], b=[3,4], 则执行a=a+b语句后, a的值是？（ ）(2分)

- A. `[1,2]`
- B. `[4,6]`
- C. `[1,2,3,4]` [+若,则执行a=a+b语句后,a的值是？，代码正确的是C]
- D. `[3,4]`
:::
[+若,则执行a=a+b语句后,a的值是？，代码正确的是C]:
    :::note 解析
    - `a = [1, 2]`: 初始化列表 `a`。
    - `b = [3, 4]`: 初始化列表 `b`。
    - `a = a + b`: 执行列表连接操作。在 Python 中，`+` 运算符用于连接两个列表，将第二个列表的元素附加到第一个列表的末尾，生成一个新的列表。
    - `a + b` 的结果是 `[1, 2] + [3, 4]`，即 `[1, 2, 3, 4]`。
    - 这个新列表 `[1, 2, 3, 4]` 被赋值回变量 `a`。
    - 因此，执行语句后 `a` 的值是 `[1, 2, 3, 4]`，对应选项 C。
    :::

:::important 单选题21
小明用列表s存储小组同学的身高, `s=[136.0,135.0,142.0,140.0]`, 则max(s)的值是？（ ）(2分)

- A. 136.0
- B. 135.0
- C. 142.0 [+小明用列表s存储小组同学的身高,则max(s)的值是？，代码正确的是C]
- D. 140.0
:::
[+小明用列表s存储小组同学的身高,则max(s)的值是？，代码正确的是C]:
    :::note 解析
    - `s = [136.0, 135.0, 142.0, 140.0]`: 定义了一个包含四个浮点数（身高）的列表 `s`。
    - `max(s)`: 调用 Python 的内置函数 `max()`，用于找出列表 `s` 中的最大值。
    - 比较列表中的元素：136.0, 135.0, 142.0, 140.0。其中最大的数字是 142.0。
    - 因此，`max(s)` 的值是 `142.0`，对应选项 C。
    :::

:::important 单选题22
已知list8列表有8个元素, 执行print(list8[1:-2])输出的结果是一个列表, 则输出的列表有几个元素？（2分）

- A. 4
- B. 5 [+已知list8列表有8个元素,执行输出的结果是一个列表,则输出的列表有几个元素？，代码正确的是B]
- C. 6
- D. 7
:::
[+已知list8列表有8个元素,执行输出的结果是一个列表,则输出的列表有几个元素？，代码正确的是B]:
    :::note 解析
    - 题目涉及 Python 列表切片操作。
    - `list8` 有 8 个元素，其有效索引范围是 0 到 7。
    - 切片 `list8[1:-2]` 的含义是：
        - 起始索引 `1` (包含)：从列表的第 2 个元素开始（索引为 1）。
        - 结束索引 `-2` (不包含)：负数索引从列表末尾开始计数，`-1` 是最后一个元素，`-2` 是倒数第二个元素。切片操作会取到结束索引 *之前* 的元素。对于有 8 个元素的列表，索引 -2 对应的是索引 6 (因为 8 - 2 = 6)。所以切片实际结束于索引 5。
    - 因此，切片 `list8[1:-2]` 包含的元素是索引为 1, 2, 3, 4, 5 的元素。
    - 元素的个数是 5 - 1 + 1 = 5 个。
    - 所以，输出的列表有 5 个元素，对应选项 B。
    :::

:::important 单选题23
下面的d是一个字典变量，能够输出数字9的语句是？（ ）(2分)
`d = {'奥运奖牌':{'金牌':5,'银牌':4},'金牌':9,'银牌':4}`

- A. `print(d['奥运奖牌']['金牌'])`
- B. `print(d['金牌'])` [+下面的d是一个字典变量，能够输出数字9的语句是？，代码正确的是B]
- C. `print(d['奥运奖牌'][-1])`
- D. `print(d['金牌'][1])`
:::
[+下面的d是一个字典变量，能够输出数字9的语句是？，代码正确的是B]:
    :::note 解析
    - 题目给出的字典是 `d = {'奥运奖牌':{'金牌':5,'银牌':4},'金牌':9,'银牌':4}`。
    - 这是一个包含嵌套字典和普通键值对的字典。
    - 我们需要找到输出数字 `9` 的语句。
    - 观察字典 `d`，键 `'金牌'` 直接对应的值就是 `9`。
    - A. `print(d['奥运奖牌']['金牌'])`: `d['奥运奖牌']` 返回嵌套字典 `{'金牌':5,'银牌':4}`，再用 `['金牌']` 访问得到 `5`。
    - B. `print(d['金牌'])`: 直接使用键 `'金牌'` 访问字典 `d`，得到对应的值 `9`。
    - C. `print(d['奥运奖牌'][-1])`: `d['奥运奖牌']` 返回一个字典，字典不能使用整数索引 `-1` 来访问，会引发错误。
    - D. `print(d['金牌'][1])`: `d['金牌']` 返回整数 `9`，整数不能使用索引 `[1]` 来访问，会引发 `TypeError`。
    - 因此，能够输出数字 `9` 的语句是 `print(d['金牌'])`，对应选项 B。
    :::

:::important 单选题24
设有元组`tup=(5,2,8,1,89,43,7,22)`,执行下列语句，结果是？（ ）
```python
tup1=sorted(tup)
print(tup1)
```
(2分)

- A. `[89, 43, 22, 8, 7, 5, 2, 1]`
- B. `(89, 43, 22, 8, 7, 5, 2, 1)`
- C. `[1, 2, 5, 7, 8, 22, 43, 89]` [+设有元组tup=...,执行下列语句，结果是？，代码正确的是C]
- D. `(1, 2, 5, 7, 8, 22, 43, 89)`
:::
[+设有元组tup=...,执行下列语句，结果是？，代码正确的是C]:
    :::note 解析
    - `tup = (5, 2, 8, 1, 89, 43, 7, 22)`: 定义了一个包含多个整数的元组 `tup`。
    - `tup1 = sorted(tup)`: 调用内置函数 `sorted()` 对元组 `tup` 进行排序。
        - `sorted()` 函数接受一个可迭代对象（如元组、列表等）作为输入。
        - 它返回一个新的 **列表**，其中包含原可迭代对象的所有元素，并按升序排列。
        - 原始的元组 `tup` 保持不变。
        - 排序后的结果是 `[1, 2, 5, 7, 8, 22, 43, 89]`。这个新列表被赋值给变量 `tup1`。
    - `print(tup1)`: 打印变量 `tup1` 的值。
    - 因此，最终输出的是排序后的列表 `[1, 2, 5, 7, 8, 22, 43, 89]`，对应选项 C。注意 `sorted()` 返回的是列表（用方括号 `[]` 表示），而不是元组。
    :::

:::important 单选题25
下列代码输出的结果是？（ ）
```python
tup=('python',2022,'神州十三号')
print(type(tup))
```
(2分)

- A. `<class 'dict'>`
- B. `<class 'set'>`
- C. `<class 'list'>`
- D. `<class 'tuple'>` [+下列代码输出的结果是？，代码正确的是D]
:::
[+下列代码输出的结果是？，代码正确的是D]:
    :::note 解析
    - `tup = ('python', 2022, '神州十三号')`: 这行代码定义了一个变量 `tup`。变量的值是用圆括号 `()` 包裹、由逗号 `,` 分隔的多个元素（一个字符串、一个整数、一个字符串）。这种数据结构在 Python 中被称为元组 (tuple)。
    - `print(type(tup))`: 调用内置函数 `type()` 来获取变量 `tup` 的数据类型，并使用 `print()` 函数将其输出。
    - `type()` 函数会返回对象的类。对于一个元组对象，`type()` 函数返回的结果是 `<class 'tuple'>`。
    - 因此，最终输出结果是 `<class 'tuple'>`，对应选项 D。
    :::
### 2212判断题
:::important 判断题1
字典的元素可以通过键来访问，也可以通过索引（下标）来访问。（ ）(2分)

- 正确
- 错误 [+字典的元素可以通过键来访问，也可以通过索引（下标）来访问。，代码正确的是错误]
:::
[+字典的元素可以通过键来访问，也可以通过索引（下标）来访问。，代码正确的是错误]:
    :::note 解析
    - Python 字典 (dict) 是一种映射类型，它的元素是键值对 (key-value pairs)。
    - 访问字典中的值 **必须** 使用对应的键 (key)，例如 `my_dict['key']`。
    - 字典是无序的（在 Python 3.7 之前）或根据插入顺序排序的（Python 3.7 及之后），但它们 **不支持** 通过数字索引（下标）像列表或元组那样访问元素（例如 `my_dict[0]`），除非字典的键恰好是整数 0, 1, 2...。
    - 因此，该说法“也可以通过索引（下标）来访问”是 **错误** 的。
    :::

:::important 判断题2
Python中，可以通过索引来访问列表、字符串等序列，也可以通过索引对列表中的元素和字符串中的字符进行修改。例如：`a='python'`, `a[0]='P'`, 字符串a将被修改为`“Python”`。（ ）(2分)

- 正确
- 错误 [+Python中，可以通过索引来访问列表、字符串等序列...，代码正确的是错误]
:::
[+Python中，可以通过索引来访问列表、字符串等序列...，代码正确的是错误]:
    :::note 解析
    - Python 中，列表 (list) 和字符串 (string) 都是序列，都可以通过索引来访问其元素或字符。例如 `my_list[0]` 或 `my_string[0]`。
    - 然而，列表是 **可变 (mutable)** 的，可以通过索引赋值来修改其元素，例如 `my_list[0] = new_value`。
    - 字符串是 **不可变 (immutable)** 的，**不能** 通过索引赋值来修改其字符。尝试执行 `a='python'; a[0]='P'` 会导致 `TypeError` 错误。
    - 题目中的例子试图修改字符串 `a` 的第一个字符，这是不允许的，字符串 `a` 不会被修改。
    - 因此，该说法“也可以通过索引对列表中的元素和字符串中的字符进行修改”是 **错误** 的。
    :::

:::important 判断题3
break用来跳出最内层for或者while循环，脱离该循环后程序继续执行后续代码。（ ）(2分)

- 正确 [+break用来跳出最内层for或者while循环...，代码正确的是正确]
- 错误
:::
[+break用来跳出最内层for或者while循环...，代码正确的是正确]:
    :::note 解析
    - `break` 语句在 Python 中的作用确实是用来立即终止其所在的 **最内层** 的 `for` 或 `while` 循环。
    - 当 `break` 语句被执行时，循环会提前结束，程序的控制流会跳转到该循环体之后的第一条语句继续执行。
    - 因此，该描述是 **正确** 的。
    :::

:::important 判断题4
单分支结构是用if语句判断满足一个条件，就执行相应的处理代码。（ ）(2分)

- 正确 [+单分支结构是用if语句判断满足一个条件...，代码正确的是正确]
- 错误
:::
[+单分支结构是用if语句判断满足一个条件...，代码正确的是正确]:
    :::note 解析
    - 单分支结构（也称为简单 `if` 语句）的基本形式是：
      ```python
      if condition:
          # 如果 condition 为 True，则执行这里的代码
          statement(s)
      ```
    - 这段代码首先判断 `condition` 是否为真 (True)。
    - 如果 `condition` 为真，则执行 `if` 下方缩进的代码块（处理代码）。
    - 如果 `condition` 为假 (False)，则跳过 `if` 下方的代码块，不执行任何操作，直接继续执行 `if` 结构之后的代码。
    - 题目的描述“用if语句判断满足一个条件，就执行相应的处理代码”准确地概括了单分支结构的行为。
    - 因此，该描述是 **正确** 的。
    :::

:::important 判断题5
tem1=(1), 用type(tem1)检查其类型应该为tuple。（ ）(2分)

- 正确
- 错误 [+tem1=(1),用type(tem1)检查其类型应该为tuple。，代码正确的是错误]
:::
[+tem1=(1),用type(tem1)检查其类型应该为tuple。，代码正确的是错误]:
    :::note 解析
    - 在 Python 中，虽然圆括号 `()` 用于定义元组 (tuple)，但当括号内只有一个元素时，需要在这个元素后面加上一个逗号 `,` 才能将其识别为单元素元组。
    - `tem1 = (1)` 这行代码中，`(1)` 仅仅表示整数 `1`，括号在这里起分组作用，并不会创建元组。因此，`tem1` 的值是整数 `1`。
    - 使用 `type(tem1)` 检查 `tem1` 的类型会返回 `<class 'int'>`，表示它是整数类型。
    - 要创建一个包含单个元素 `1` 的元组，应该写成 `tem1 = (1,)`。
    - 因此，题目中“用type(tem1)检查其类型应该为tuple”的说法是 **错误** 的。
    :::

:::important 判断题6
在语句`for i in range(5):`中，当进入第5次循环时，i的值为5。（ ）(2分)

- 正确
- 错误 [+在语句foriinrange(5):中...，代码正确的是错误]
:::
[+在语句foriinrange(5):中...，代码正确的是错误]:
    :::note 解析
    - `range(5)` 函数生成一个从 0 开始，到 5 结束（但不包含 5）的整数序列，即 `0, 1, 2, 3, 4`。
    - `for i in range(5):` 循环会依次将 `range(5)` 生成的序列中的每个值赋给变量 `i`。
    - 循环的执行过程如下：
        - 第 1 次循环：`i` 的值为 0。
        - 第 2 次循环：`i` 的值为 1。
        - 第 3 次循环：`i` 的值为 2。
        - 第 4 次循环：`i` 的值为 3。
        - 第 5 次循环：`i` 的值为 4。
    - 循环总共执行 5 次，`i` 的值依次是 0, 1, 2, 3, 4。
    - 题目中说“当进入第5次循环时，i的值为5”，这与实际情况（第5次循环时 `i` 的值为 4）不符。
    - 因此，该说法是 **错误** 的。
    :::

:::important 判断题7
clear()方法可以移除列表中第一次出现的该元素。（ ）(2分)

- 正确
- 错误 [+clear()方法可以移除列表中第一次出现的该元素。，代码正确的是错误]
:::
[+clear()方法可以移除列表中第一次出现的该元素。，代码正确的是错误]:
    :::note 解析
    - Python 列表的 `clear()` 方法用于 **移除列表中的所有元素**，使列表变为空列表。它不接受任何参数。
    - 移除列表中 **第一次出现的指定元素** 的方法是 `remove(x)`，其中 `x` 是要移除的元素的值。
    - 因此，题目中关于 `clear()` 方法作用的描述是 **错误** 的。
    :::

:::important 判断题8
运行如下代码,结果为：`“cba321”`。（ ）(2分)
```python
words = "123abc"
slice1=words[-1::-1]
print(slice1)
```
- 正确 [+运行如下代码：结果为：“cba321”。，代码正确的是正确]
- 错误
:::
[+运行如下代码：结果为：“cba321”。，代码正确的是正确]:
    :::note 解析
    - `words = "123abc"`: 定义了一个字符串变量 `words`。
    - `slice1 = words[-1::-1]`: 这行代码使用了字符串切片来反转字符串。
        - `[-1::-1]` 是切片语法 `[start:stop:step]` 的一种形式。
        - `start = -1`: 表示从字符串的最后一个字符开始（索引为 -1，即 'c'）。
        - `stop` 省略：当 `step` 为负数时，省略 `stop` 表示切片一直到字符串的开头。
        - `step = -1`: 表示从右向左，每次移动一个字符。
    - 这个切片操作会从最后一个字符 'c' 开始，依次向前取字符，直到字符串的第一个字符 '1'。
    - 因此，`slice1` 的值将是 `"cba321"`。
    - `print(slice1)`: 打印 `slice1` 的值。
    - 题目中说结果为 `"cba321"`，这与代码实际执行的结果一致。
    - 因此，该描述是 **正确** 的。
    :::

:::important 判断题9
元组中的元素值是不允许删除的，但我们可以使用del语句来删除整个元组。（ ）(2分)

- 正确 [+元组中的元素值是不允许删除的...，代码正确的是正确]
- 错误
:::
[+元组中的元素值是不允许删除的...，代码正确的是正确]:
    :::note 解析
    - Python 中的元组 (tuple) 是 **不可变 (immutable)** 数据类型。这意味着一旦创建了元组，就不能修改、添加或 **删除** 其内部的单个元素。所以，“元组中的元素值是不允许删除的” 这部分是正确的。
    - 然而，`del` 语句是 Python 中用于删除 **对象引用**（变量名）或从可变容器（如列表、字典）中删除元素的关键字。
    - 我们可以使用 `del` 语句删除指向整个元组对象的变量名。例如，如果有一个元组 `my_tuple = (1, 2, 3)`，执行 `del my_tuple` 后，变量名 `my_tuple` 将被删除，之后再尝试访问 `my_tuple` 会导致 `NameError`。这有效地“删除”了对该元组的引用（如果这是唯一的引用，元组对象本身随后会被垃圾回收）。
    - 因此，“我们可以使用del语句来删除整个元组” 这部分也是正确的（指删除对元组的引用）。
    - 综上所述，整个描述是 **正确** 的。
    :::

:::important 判断题10
else保留字除了可以和if语句一起搭配使用构成if...else..., 也可以和while或for循环搭配使用，构成while...else或for...else。（ ）(2分)

- 正确 [+else保留字除了可以和if语句一起搭配使用...，代码正确的是正确]
- 错误
:::
[+else保留字除了可以和if语句一起搭配使用...，代码正确的是正确]:
    :::note 解析
    - Python 中的 `else` 关键字确实有多种用途。
    - **与 `if` 语句搭配**: 这是最常见的用法，构成 `if...elif...else` 结构，用于在 `if` 和所有 `elif` 条件都不满足时执行代码块。
    - **与 `while` 循环搭配**: `while` 循环可以带有一个 `else` 子句。这个 `else` 子句在循环条件变为假（即循环正常结束）时执行。如果循环被 `break` 语句中断，则 `else` 子句 **不会** 执行。
    - **与 `for` 循环搭配**: `for` 循环也可以带有一个 `else` 子句。这个 `else` 子句在 `for` 循环遍历完所有项（即循环正常结束）时执行。如果循环被 `break` 语句中断，则 `else` 子句 **不会** 执行。
    - 题目中描述了 `else` 可以与 `if`, `while`, `for` 搭配使用，这是完全正确的。
    - 因此，该描述是 **正确** 的。
    :::

### 2212编程题
::::important 1. 编写一个重复执行的程序 (15分)
- 要求用户输入一个字符串。如果输入的字符串的长度是奇数，就输出字符串最中间的字符。如果字符串的长度是偶数，就输出字符串的最后一个字符。然后重复以上操作。
:::code-tabs
@tab Python
```python :collapsed-lines=5
# 编写一个重复执行的程序 (15分)
# 要求用户输入一个字符串。
# 如果输入的字符串的长度是奇数，就输出字符串最中间的字符。
# 如果字符串的长度是偶数，就输出字符串的最后一个字符。
# 然后重复以上操作。

while True:
    try:
        # 获取用户输入
        user_input = input("请输入一个字符串 (或直接按 Enter 结束): ")

        # 如果用户直接按 Enter，则结束循环
        if not user_input:
            print("程序结束。")
            break

        # 获取字符串长度
        length = len(user_input)

        # 判断长度是奇数还是偶数
        if length % 2 != 0:
            # 长度为奇数，输出中间字符
            middle_index = length // 2
            print(f"中间字符是: {user_input[middle_index]}")
        else:
            # 长度为偶数，输出最后一个字符
            print(f"最后一个字符是: {user_input[-1]}")

    except EOFError:
        # 处理可能的输入结束情况 (例如在某些环境中使用 Ctrl+D)
        print("\n程序结束。")
        break
    except Exception as e:
        # 处理其他可能的错误
        print(f"发生错误: {e}")
        break
```
@tab Python
```python :collapsed-lines=5
# 获取用户输入
user_input = input("请输入一个字符串 (或直接按 Enter 结束): ")

# 如果用户直接按 Enter，则结束循环
if not user_input:
    print("程序结束。")
    break

# 获取字符串长度
length = len(user_input)

# 判断长度是奇数还是偶数
if length % 2 != 0:
    # 长度为奇数，输出中间字符
    middle_index = length // 2
    print(f"中间字符是: {user_input[middle_index]}")
else:
    # 长度为偶数，输出最后一个字符
    print(f"最后一个字符是: {user_input[-1]}")
```
:::
::::

::::important 2. 输入字符串 (15分)
- 从键盘输入任意字符串，按照下面要求分离字符串中的字符：
    1. 分别取出该字符串的第偶数位的元素（提醒注意：是按照从左往右数的方式确定字符串的位置）；
    2. 并依次存储到一个列表中；
    2. 输出这个列表。
:::code-tabs
@tab Python
```python :collapsed-lines=5
# 获取用户输入
input_string = input("请输入一个字符串: ")

# 方法一：使用循环和索引判断
# even_position_chars = []
# for i in range(len(input_string)):
#     # 索引从0开始，所以第偶数位对应的是奇数索引 (1, 3, 5, ...)
#     if (i + 1) % 2 == 0: # 或者直接判断索引 i 是否为奇数: if i % 2 != 0:
#         even_position_chars.append(input_string[i])

# 方法二：使用切片 (更简洁)
# 切片 [1::2] 表示从索引 1 开始，取到末尾，步长为 2
# 这正好取出了索引为 1, 3, 5, ... 的字符，即第 2, 4, 6, ... 位的字符
even_position_chars_str = input_string[1::2]
# 将切片得到的字符串转换为列表
even_position_chars_list = list(even_position_chars_str)

# 输出结果列表
print(even_position_chars_list)
```
:::
::::